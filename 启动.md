# 启动命令

## 编译

```bash
cd /home/yuan/ibvs_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install
```

## 终端 1：DDS Agent

```bash
source /opt/ros/humble/setup.bash
source /home/yuan/px4_ros2_ws/install/setup.bash
MicroXRCEAgent udp4 -p 8888
```

## 终端 2：PX4 与 Gazebo

```bash
cd /home/yuan/PX4-Autopilot
export PX4_GZ_GUI_CONFIG=/home/yuan/ibvs_ws/src/ibvs_sim/config/camera_3d_and_first_person.config
make px4_sitl gz_x500_mono_cam
```

Gazebo GUI 同时显示 `IBVS Gazebo 3D` 第三人称场景和
`IBVS Camera — First Person` 前视相机。该环境变量只替换 GUI 配置，不改变
PX4 控制、Gazebo 机载相机或 ROS 图像话题；修改后需重启 PX4/Gazebo。

该仿真模型的相机与机体刚性固定、光轴沿机体前向，`mono_cam` 的水平视场角为
`120°`，并在镜头中心设置了专用碰撞体。修改模型后需重启该终端中的 PX4/Gazebo，
不能沿用已经运行的旧模型实例。

同时，x500 四个旋翼的 `motorConstant` 已标定为 `1.4709975e-05`，在 `maxRotVelocity=1000`
下对应总推力 `Tmax=58.8399 N`。整机仿真质量还包含四个旋翼链接和相机，
共 `2.1143076923 kg`。控制器同时考虑 PX4 的 `150..1000 rad/s`
输出区间和二次推力映射；不要
只修改 YAML 而不重启 Gazebo，否则被控对象仍会使用旧的推力模型。

## 终端 3：目标与视觉检测

静止目标（默认目标中心高度 `4.0 m`，与相机光轴同高）：

```bash
cd /home/yuan/ibvs_ws
source /opt/ros/humble/setup.bash
source /home/yuan/px4_ros2_ws/install/setup.bash
source /home/yuan/ibvs_ws/install/setup.bash
ros2 launch ibvs_sim p4_vision_monitor.launch.py target_z:=4.0
```

如修改了控制器的 `target_altitude_m`，应同时显式传入相同高度，例如
`target_z:=4.0`，否则会重新产生上下偏置的撞击线。

该入口默认按论文实验引入 `80 ms` 图像总延迟；对应 observer 的 `50 Hz` DKF
更新周期为 `D=4`。需要做无延迟回归对照时可显式传入 `image_delay_s:=0.0`，但正式
延迟验收不要覆盖默认值。

论文 HITL 移动目标

```bash
cd /home/yuan/ibvs_ws
source /opt/ros/humble/setup.bash
source /home/yuan/px4_ros2_ws/install/setup.bash
source /home/yuan/ibvs_ws/install/setup.bash
ros2 launch ibvs_sim p7_moving_target.launch.py \
  target_z:=4.0 \
  target_pattern:=figure8 \
  target_speed_m_s:=2.0
```

移动目标在视觉搜索和对准期间保持初始位置，收到
`/interception/observer/reset` 后才开始运动。目标运动只写入 Gazebo plant，
不向控制器发布目标位姿、速度或 odometry。

## 终端 4：视觉拦截控制

静止目标和移动目标都使用论文链路：控制器的 $p_r$、$v_r$ 和图像状态
全部来自 DKF：

```bash
cd /home/yuan/ibvs_ws
source /opt/ros/humble/setup.bash
source /home/yuan/px4_ros2_ws/install/setup.bash
source /home/yuan/ibvs_ws/install/setup.bash
ros2 launch ibvs_control vision_direct_interception_trial.launch.py \
  trial_id:=paper_static_$(date +%Y%m%d_%H%M%S) \
  enable_flight_commands:=true \
  confirmation_token:=ENABLE_VISION_ONLY_CONTROL \
  speed_limit_m_s:=21.0 \
  interception_tilt_limit_deg:=55.0 \
  max_horizontal_distance_m:=15.0 \
  record_bag:=true
```

移动目标通过静止目标验收后再使用：

```bash
cd /home/yuan/ibvs_ws
source /opt/ros/humble/setup.bash
source /home/yuan/px4_ros2_ws/install/setup.bash
source /home/yuan/ibvs_ws/install/setup.bash
ros2 launch ibvs_control vision_direct_interception_trial.launch.py \
  trial_id:=paper_moving_2ms_$(date +%Y%m%d_%H%M%S) \
  enable_flight_commands:=true \
  confirmation_token:=ENABLE_VISION_ONLY_CONTROL \
  speed_limit_m_s:=21.0 \
  interception_tilt_limit_deg:=55.0 \
  max_horizontal_distance_m:=20.0 \
  record_bag:=true
```

`speed_limit_m_s` 和 `interception_tilt_limit_deg` 都是独立安全终止门限，不会
改写论文控制指令。论文静态目标实验最高速度为 `21 m/s`，因此论文复现基线使用
`21.0 m/s`；实际带飞仍建议先通过 `5/10/15 m/s` 档位验收。论文实验报告最大俯仰
约 `50°`；当前使用 `55°` 作为独立终止门限，同时保留 `60°` 的整机最终倾角保护。

控制器直接执行论文式 (19)、(21)、(23) 和 (28)；旧的静态目标旁路、加速度限幅、
力方向倾角投影、最小推力、指令低通和倾角回正代码均已删除。

起飞到 `target_altitude_m` 并稳定后，无人机会先保持位置并绕偏航轴搜索。
当前相机中心拦截基线要求 `target_altitude_m=4.0`、`target_z=4.0`、
`designed_los_pitch_deg=0.0` 三者一致；目标红色质心、设计视线和镜头碰撞体
因此共线。
目标不在视野内或图像过期时不会进入拦截；只有目标持续对准、observer
重置后重新有效且控制指令新鲜时，才会切换到拦截预流和主动控制。
