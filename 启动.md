# 纯图像 IBVS 拦截（无人工延迟）

按顺序在四个终端运行。

## 终端 1：DDS Agent

```bash
source /opt/ros/humble/setup.bash
source ~/px4_ros2_ws/install/setup.bash
MicroXRCEAgent udp4 -p 8888
```

## 终端 2：PX4 + Gazebo 前视相机

```bash
cd ~/PX4-Autopilot
make px4_sitl gz_x500_mono_cam
```

`PX4_HOME_LAT/LON/ALT` 可以作为可重复实验条件任意设置，但不再是成功拦截的
前提。起飞后控制器会保持本机位置并通过相机执行 `VISUAL_SEARCH ->
VISUAL_ALIGN`，而不用固定世界航向代表目标方向。

## 终端 3：目标、相机与视觉特征

```bash
cd ~/ibvs_ws
source /opt/ros/humble/setup.bash
source ~/px4_ros2_ws/install/setup.bash
source ~/ibvs_ws/install/setup.bash
ros2 launch ibvs_sim p4_vision_monitor.launch.py
```

该启动文件发布相机图像和红色目标的像素中心、归一化坐标及面积。
它也启动 truth/contact 评估旁路，但这些话题只用于录包、碰撞确认和误差分析；
控制器不订阅它们。

## 终端 4：纯视觉闭环试验

```bash
cd ~/ibvs_ws
source /opt/ros/humble/setup.bash
source ~/px4_ros2_ws/install/setup.bash
source ~/ibvs_ws/install/setup.bash
ros2 launch ibvs_control vision_direct_interception_trial.launch.py \
  trial_id:=vision_only_01 \
  enable_flight_commands:=true \
  confirmation_token:=ENABLE_VISION_ONLY_CONTROL \
  record_bag:=true
```

控制链为：

```text
/camera/image_raw
  -> red_target_detector
  -> /interception/vision/raw_feature
  -> VISUAL_SEARCH -> VISUAL_ALIGN -> IBVS
  -> vision_interception_coordinator
  -> /fmu/in/vehicle_rates_setpoint
  -> PX4
```

`VISUAL_ALIGN` 只是起飞后的预对准，不是全部拦截控制。进入 `ACTIVE`
后仍会逐帧用 `(x_norm, y_norm, area_px)` 更新 IBVS 角速度、接近速度和
横向修正；横向修正只使用图像 LOS 与无人机自身的 PX4 速度，不使用目标
世界坐标、目标速度或真实距离。

运行时检查：

```bash
ros2 topic echo /interception/vision/raw_feature --once
ros2 topic echo /interception/control/debug --once
ros2 node info /vision_interception_coordinator
```

前两项应分别出现 `valid: true` 和
`reason: ok_visual_features_only`。最后一项的订阅列表中可以有 PX4 自身状态和
`/interception/vision/raw_feature`，但不得出现
`/interception/truth/relative_state`、目标里程计或 Gazebo contact。

目标图像面积达到终端阈值且图像误差满足要求后，控制器立即退出 IBVS。如果旋翼
先触发红绿变色，使红色分割在面积终端阈值前突然失效，还会根据最近 0.30 s 内的
纯图像历史（已对准且面积比 >= 0.04）识别“近距离目标突然消失”。该备用判据不读取
contact、truth 或目标三维状态。为了保证 setpoint 连续，它先短暂保持进入终端阶段
时的速度，再用零端点加速度曲线平滑减速，
完整状态依次为：

```text
VISUAL_SEARCH -> VISUAL_ALIGN -> PRESTREAM -> ACTIVE
-> IMPACT_DETECTED -> EXIT_INTERCEPTION -> STABILIZE -> HOVER
```

`/interception/control/debug` 在 IBVS 退出后仍会记录姿态、机体角速度、速度、图像误差、
检测状态、终端状态、控制状态和过渡指令。真实 contact 和目标变绿只作为实验是否确实
发生碰撞的评估证据，不触发控制状态切换。
