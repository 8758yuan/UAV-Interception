# 当前主要问题及解决方案

> 当前按照论文《High-Speed Interception Multicopter Control by Image-Based Visual Servoing》进行仿真复现。
> 
> 当前已在 18 维状态观测器上加入论文 DKF 延迟处理。SITL 图像采集到发布的总延迟设为论文实测约 `80 ms`；按 `50 Hz` DKF 更新周期计算 `D=4`。`200 Hz` IMU 只作为每个 DKF 周期内的 4 个传播子步。

# 当前开发顺序

```text
① 删除 Truth 相关模块（完成）
        ↓
② 建立论文定义的 18 维状态（完成）
        ↓
③ 实现 D=4 延迟状态回溯、图像校正和 IMU 重传播（完成）
        ↓
④ 验证 18 维状态估计（自动化验证完成）
        ↓
⑤ 对齐论文 Section III / Algorithm 1 控制器（完成）
        ↓
⑥ 完成静止目标延迟闭环拦截（无延迟基线完成，D=4 待 SITL 复验）
        ↓
⑦ 加入论文移动目标（暂停，待静止目标重新通过）
```

---

## 1. 删除 Truth 相关模块

### 当前问题

当前项目中仍存在与目标真实状态有关的模块，例如：

```text
目标 Gazebo odometry
Truth State
Truth 相对位置 p_r
Truth 相对速度 v_r
Truth Controller / Shadow 等相关逻辑
```

论文中的目标真实位置和真实速度不是实际拦截系统可以直接获得的控制输入，因此最终系统不能依赖这些 Truth 信息。

### 修改要求

删除与目标 Truth 状态获取和使用有关的：

```text
文件
节点
topic
launch 配置
控制器接口
```

最终在线系统中不直接读取：

```text
目标真实位置
目标真实速度
目标 Gazebo odometry
Truth p_r
Truth v_r
```

注意：

论文中仍然定义了相对位置 `p_r` 和相对速度 `v_r`，并将它们作为状态观测器的一部分。

因此：

```text
p_r、v_r 作为“估计状态”存在      → 正确

Gazebo Truth 直接提供 p_r、v_r   → 禁止
```

### 当前阶段范围

当前只处理 Truth。

`Contact` 暂时保持现状，不在这一阶段检查、删除或修改。

---

## 2. 建立论文定义的 18 维状态

论文定义状态：

```text
x = [qᵀ, p_rᵀ, v_rᵀ, p̄ᶦᵀ, b_gyrᵀ, b_accᵀ]ᵀ ∈ R¹⁸
```

具体组成：

| 状态      | 维数     | 含义          |
| ------- | ------:| ----------- |
| `q`     | 4      | 无人机姿态四元数    |
| `p_r`   | 3      | 无人机与目标的相对位置 |
| `v_r`   | 3      | 无人机与目标的相对速度 |
| `p̄ᶦ`   | 2      | 归一化目标图像坐标   |
| `b_gyr` | 3      | 陀螺仪偏置       |
| `b_acc` | 3      | 加速度计偏置      |
| **合计**  | **18** |             |

这里的 `p_r`、`v_r` 必须属于 observer 内部估计状态，不能从 Gazebo Truth 获取。

这一阶段首先建立：

（公式见论文附录）

```text
18维状态结构
状态初始化
协方差 P
过程噪声 Q
图像测量噪声 R
状态转移所需的数据接口
```

### 实现结果

已建立严格按论文顺序排列的状态结构：

```text
索引 0:4    q
索引 4:7    p_r
索引 7:10   v_r
索引 10:12  p̄ᶦ
索引 12:15  b_gyr
索引 15:18  b_acc
```

ROS 输出为 `/interception/observer/state`，消息包含全部 18 维状态、
`18×18` 协方差和传播/校正计数。

论文将 `x(0)、P、Q、R` 作为已知输入，但没有给出可直接照抄的数值。
当前初值结合论文模型、12 m 仿真几何和常见小型无人机 IMU 量级定义：

| 项目 | 当前值 |
| --- | ---: |
| 初始姿态 `q` | 首个 PX4 自身姿态，仅用于 `x(0)` |
| 初始 `p_r` | 首帧图像 LOS + 12 m 光轴深度先验 |
| 初始 `v_r` | `[0, 0, 0] m/s` |
| 初始 `b_gyr` | `[0, 0, 0] rad/s` |
| 初始 `b_acc` | `[0, 0, 0] m/s²` |
| `P` 姿态标准差 | `0.005` |
| `P` 位置标准差 | `0.5 m` |
| `P` 速度标准差 | `0.2 m/s` |
| `P` 图像标准差 | `0.02` |
| `P` 陀螺偏置标准差 | `0.005 rad/s` |
| `P` 加计偏置标准差 | `0.05 m/s²` |
| `Q` 陀螺标准差 | `0.015 rad/s` |
| `Q` 加计标准差 | `0.15 m/s²` |
| `R` 图像标准差 | `0.015` |

当前图像更新还使用二维创新卡方门限 `NIS ≤ 9.21`：通过门限的帧仍按论文公式执行
完整 EKF 更新；异常帧保持预测结果，避免异常/滞后图像通过协方差交叉项污染 `p̂_r`
和 `v̂_r`。

这组 `P/R` 已根据失败 bag 修正。旧配置把 12 m 标定深度同时声明为 `3 m`
位置不确定度，并把像素测量噪声压到 `0.005`，使二维 bearing 残差通过 Kalman
交叉协方差虚构了约 10 m 的径向位移。新配置仍执行论文的完整 18 维 Kalman 更新，
只修正论文未公开的数值参数，没有删除 `p_r/v_r/q/bias` 的校正项。

初始深度只是参数先验，不订阅或读取目标 Gazebo 位置。

---

## 3. 实现 D=4 状态传播与延迟图像更新

论文 DKF 使用：

```text
IMU measurement
        +
delayed image measurement
        ↓
18维状态估计
```

论文飞行实验给出的成像与处理总延迟约为 `80 ms`，实际 DKF 更新频率为
`50 Hz`，因此本项目取：

```text
T_DKF = 1 / 50 Hz = 20 ms
D = round(80 ms / 20 ms) = 4
```

这里的 `D` 是 **DKF 更新周期数**，不是 IMU 周期数。项目 IMU 为 `200 Hz`，
所以每个 DKF 周期包含约 4 个 IMU 传播子步，回放 4 个 DKF 周期时内部共保留
约 16 个 IMU 子步；这不会把 `D` 改写成 16。

### 3.1 IMU 状态传播

使用论文中的 IMU 模型进行状态 propagation。

输入主要包括：

```text
gyroscope
accelerometer
```

用于传播：

```text
q
p_r
v_r
p̄ᶦ
b_gyr
b_acc
```

并同步传播协方差：

```text
P(k|k-1)
```

### 3.2 延迟图像测量更新

相机检测得到：

```text
p_xi
p_yi
```

根据焦距归一化：

```text
p̄ᶦ = [p_xi / foc, p_yi / foc]ᵀ
```

SITL 检测器按图像 `capture_stamp` 保证采集到发布至少相隔 `80 ms`。observer
收到 `image(k-D)` 后执行：

```text
image(k-D)
      ↓
回溯到 x̂(k-D)，执行 Eq. (32)–(36)
      ↓
重放 D=4 个 DKF 周期内保存的 IMU 子步
      ↓
得到当前状态 x̂(k)
```

observer reset 会同时清空检测器中尚未发布的旧图像，避免拦截开始后使用 reset
之前采集的帧。启动阶段历史不足 4 个 DKF 周期时只进行 IMU 预测，历史就绪后再做
第一次延迟校正。

### 实现结果

IMU 非线性传播使用论文附录 B/C：

```text
q       Eq. (44)，Appendix C 的 M(Δq)
p_r     Eq. (46)
v_r     Eq. (48)
p̄ᶦ     Eq. (51)
F、G    Eq. (45)、(47)、(49)、(50)、(52)–(55)
P       Eq. (30)、(31)
```

历史图像在 `k-D` 执行 Eq. (32)–(36) 更新，测量矩阵为：

```text
H = [0₂×₄  0₂×₃  0₂×₃  I₂×₂  0₂×₃  0₂×₃]
```

坐标统一为 Earth ENU、Body FLU、Camera right-down-forward。PX4 IMU 的
FRD 数据进入 observer 前先转换到 FLU。

论文排版中的 Eq. (55) 未显示 `Δt`，但它是离散 Eq. (51) 对陀螺偏置的
偏导；实现保留维度上必需的 `Δt`。其余传播块直接对应附录公式。

---

## 4. 验证 18 维状态估计

完成 observer 后，首先验证状态输出是否稳定、连续，并能够为论文控制器提供所需信息。

需要得到：

```text
x̂ =
[q̂,
 p̂_r,
 v̂_r,
 p̄̂ᶦ,
 b̂_gyr,
 b̂_acc]
```

重点检查：

```text
四元数 q̂ 是否归一化且连续
p̄̂ᶦ 是否能够稳定跟踪图像目标
v̂_r 是否连续、合理
p̂_r 是否能够正常传播
IMU bias 是否正常更新
状态是否出现 NaN / Inf / 发散
```

这里必须区分：

```text
18维 observer 状态
        ≠
18维状态全部直接进入控制器
```

observer 的作用是统一估计系统所需状态。

### 验证结果

自动化测试已覆盖：

```text
18维布局与索引
四元数单位范数和符号连续性
Eq. (49) 姿态 Jacobian 数值差分
Eq. (54) 图像 Jacobian 数值差分
D=0 基础图像更新及图像协方差下降
D=4 历史状态校正、约 16 个 IMU 子步重放及协方差一致性
1000 次 IMU 传播/图像校正无 NaN、Inf 或协方差失稳
非法 dt 不修改 observer 状态
```

`vision_direct_interception_trial.launch.py` 会同时启动 observer，并把
`/fmu/out/sensor_combined` 与 `/interception/observer/state` 录入试验 bag。
当前控制器已经读取 observer 输出；具体数据流见下一节第 5 点。

---

## 5. 对齐论文 Section III / Algorithm 1 控制器（已完成）

论文 Algorithm 1 的控制器接口写作：

```text
InterceptionController(p̄ᶦ, v_r, Rᵉ_b)
              ↓
         f_d, ω_d
```

论文的输入栏只列出了图像测量、相对速度和自身姿态，但 Eq. (19)
明确使用相对位置 `p_r`。因此本项目将 `p_r` 作为 observer 的估计量一并传入
外环；这里的 `p_r` 始终是 `p̂_r`，不是目标 Truth。控制器并不接收或使用
observer 的陀螺偏置、加速度计偏置和协方差作为控制输入。

18维状态估计完成后，从估计结果中提取控制器需要的信息：

```text
18维状态 x̂
     ↓
┌───────────────┐
│ p̄̂ᶦ          │
│ p̂_r、v̂_r    │
│ PX4 自身姿态 → Rᵉ_b │
└───────────────┘
     ↓
InterceptionController
     ↓
f_d + ω_d
```

**并不是将全部 18 个状态直接输入控制器。**

### 控制器结构

按照论文 Section III：

```text
p̄̂ᶦ
 ↓
目标 LOS n_t
 ↓
Collinear Controller
 ↓
期望加速度 a_d
期望姿态 R_d
 ↓
Attitude Controller
 ↓
ω_d

同时：
a_d
 ↓
期望推力 f_d
```

重点核对论文：

```text
Eq. (13)
Eq. (19)
Eq. (21)
Eq. (22)
Eq. (23)
Eq. (26)
Eq. (28)
```

最终输出：

```text
期望推力 f_d
期望角速度 ω_d
```

再转换为 PX4 对应的 body-rate / thrust 控制指令。

### 实现结果

已完成 observer 到论文控制器的在线数据流：

```text
/interception/observer/state
        ↓
VisionInterceptionCoordinator
        ├─ 50 Hz：Eq. (13)、(19)、(21)–(23)
        └─ 200 Hz：Eq. (26)、(28)
        ↓
FLU body-rate + physical thrust
        ↓
FLU → PX4 FRD
        ↓
/fmu/in/vehicle_rates_setpoint
```

控制器使用的唯一目标状态来源是 `/interception/observer/state`。
原始图像只继续用于视觉获取和诊断；终端退出仅由碰撞及目标变绿确认。图像不会绕过 observer 直接替代
论文控制器的目标状态输入。`p̂_r`、`v̂_r`、`p̄̂ᶦ` 来自 18 维估计；论文
Algorithm 1 要求的当前自身姿态 `Rᵉ_b` 使用 PX4 实测姿态。observer 中的 `q̂`
仍参与状态传播和诊断，但不再替代飞控已经可靠提供的当前机体姿态。控制输出在发送前
继续执行角速度范数、推力和 PX4 FRD 坐标转换约束。

### 与论文逐项对照结果

| 对照项 | 当前代码 | 结论 |
| --- | --- | --- |
| Eq. (13) LOS 角速度 | `z1 / (k_b²-z1²) · Rᵀ(n_td×n_t)` | 一致 |
| Eq. (19) 期望加速度 | 使用估计的 `p̂_r、v̂_r` 和 `n_t` | 一致 |
| Eq. (21) 推力方向 | `m(a_d-g)-f_drag` 归一化 | 一致；当前静止目标令 `f_drag=0` |
| Eq. (22) 期望姿态 | 当前推力方向到期望推力方向的 SO(3) 最短旋转 | 一致 |
| Eq. (23) 推力 | `n_fᵀ[m(a_d-g)-f_drag]` 并限幅 | 一致 |
| Eq. (26) 姿态角速度 | `-vex(R_dᵀR_b-R_bᵀR_d)` | 一致 |
| Eq. (28) 角速度/推力饱和 | 保留论文范数饱和，并增加倾角和 PX4 坐标约束 | 论文一致且有安全扩展 |
| 控制器状态来源 | `ObserverState` 的 `p̂_r、v̂_r、p̄̂ᶦ` + PX4 自身 `Rᵉ_b` | 一致；不使用目标 Truth |

需要明确三个范围边界：论文的 Algorithm 1 接口只列出图像、相对速度和姿态，
但 Eq. (19) 实际使用 `p_r`；当前实现因此从同一个 observer 取 `p̂_r`。论文没有
给出 `k1、k2` 的数值，当前 YAML 中的数值是工程调参，不是论文原始参数。论文的
核心图像延迟模型已按 `80 ms / 50 Hz` 配置为 `D=4`，并实现历史校正及 IMU
重传播。当前还需用新的延迟链路重新完成 PX4/Gazebo 飞行验收。

## 前五点完成度与论文一致性审查

| 点 | 当前状态 | 审查结论 |
| --- | --- | --- |
| 1. 删除 Truth 依赖 | 已删除目标 odometry、Truth 状态和 Truth 控制链路；控制器只订阅 observer 状态。PX4 自身 odometry 仅用于安全监控和调试 | 完成 |
| 2. 18 维状态 | `x=[q,p_r,v_r,p̄ᶦ,b_gyr,b_acc]`，顺序和维度与论文一致 | 完成 |
| 3. 状态传播/图像更新 | Appendix B/C 传播、历史状态校正和 IMU 重传播已实现；`D=4` | 完成，待 SITL 飞行复验 |
| 4. 状态估计验证 | 已覆盖 Jacobian、四元数、协方差、长时间传播、异常图像门限和 ROS 数据流 | 完成 |
| 5. Section III 控制器 | 外环 50 Hz、内环 200 Hz，Eq. (13)、(19)、(21)–(23)、(26)、(28) 已接入 | 完成；含工程安全扩展 |

因此，“前五点已完成”现在包括论文 `D>0` 延迟 DKF 的代码链路。当前代码与论文
仍存在未公开增益数值、零阻力静止目标模型以及 PX4 工程安全保护等差异。

---

## 6. 完成静止目标延迟闭环拦截（D=4 待 SITL 复验）

前五步完成后，首先使用静止目标验证完整闭环：

```text
Camera
   ↓
目标图像检测
   ↓
p̄ᶦ measurement
   ↓
18维无延迟状态观测器
   ↓
x̂
   ↓
提取 p̂_r、p̄̂ᶦ、v̂_r，并读取 PX4 自身 Rᵉ_b
   ↓
论文 InterceptionController
   ↓
f_d、ω_d
   ↓
PX4
   ↓
无人机
```

这一阶段需要确认：

```text
目标状态不来自 Truth
18维 observer 稳定
目标保持在 FOV
无人机能够持续逼近目标
推力和角速度输出正常
最终完成静止目标拦截
```

离线确定性验收继续保留 `D=0` 作为回归基线；PX4/Gazebo 主试验入口已启用
`80 ms` 图像延迟和 `D=4` DKF。

### 实现结果

新增 `observer_closed_loop.py` 作为确定性的无延迟验收模型。目标真实状态只
存在于仿真的 plant 和合成相机中，控制路径只接收：

```text
当前图像 measurement
当前 IMU
初始自身姿态
        ↓
PaperStateObserver
        ↓
p̂_r、v̂_r、p̄̂ᶦ、R̂ᵉ_b
        ↓
论文 SO(3) 控制器
```

确定性离线模型曾得到以下结果，但它不能替代 PX4/Gazebo 飞行验收：

| 指标 | 结果 |
| --- | ---: |
| 拦截距离阈值 | `0.5 m` |
| 最小距离 | `0.499 m` |
| 最大速度 | `1.358 m/s` |
| 最大倾角 | `13.554°` |
| 最大图像归一化半径 | `0.339` |
| 最大相对速度估计误差 | `0.172 m/s` |
| 传播次数 | `6902` |
| 图像校正次数 | `1035` |
| 结果 | `hit / safe` |

最新 PX4/Gazebo 记录显示旧闭环未通过：observer 在起飞前就被图像初始化，进入
拦截时 `p̂_r、v̂_r` 已漂移；同时内环错误地用漂移的 `q̂` 代替 Algorithm 1 的
当前自身姿态。旧记录中 observer 与 PX4 姿态差最大约 `64.8°`，相对位置估计范数
最大约 `62.9 m`，最终触发安全中止，不能再标记为“静止目标已完成”。

最新失败记录 `paper_moving_5ms_20260920_204927`（实际用于当前静止目标链路）中，
无人机高度保持在约 `3.76–4.19 m`，已经满足高于 3 m 目标的要求，最大速度约
`2.0 m/s`，也没有触发倾角或速度门限。但实际只前进约 `2.3 m` 时，旧 EKF 已把
估计相机深度从 `12 m` 压缩到零，触发
`estimated target depth is invalid or behind camera`。用同一 bag 重放新 `P/R` 后，
相同时间点的估计深度约为 `10.6 m`，与实际剩余距离约 `11 m` 一致。因此本轮
修复目标是 observer 的单目尺度过度校正，而不是继续放宽飞行安全门限。

后续静止目标记录 `paper_static_20260920_210750` 表明，仅调整 `P/R` 虽将失效时间
从约 4.5 s 推迟到约 26.8 s，但仍未消除长期单目尺度漂移。ACTIVE 阶段实际只前进
约 `6.3 m`，observer 最终再次把深度压到零；同时横滚/俯仰及对应角速度约每秒
换向 `0.7` 次，角速度反复接近 `±0.24 rad/s`，期望加速度反复触及
`±3 m/s²`。这正是图像中目标上下左右摆动和接近速度偏慢的直接原因。

本轮修复为：

```text
起飞 + 视觉搜索/对准
        ↓
显式发布 /interception/observer/reset
        ↓
下一帧有效图像建立论文 x(0)
        ↓
observer 提供 p̂_r、v̂_r、p̄̂ᶦ
PX4 odometry 提供当前自身 Rᵉ_b
        ↓
论文 Section III 控制器
```

角速度范数按论文式 (29) 限制为 `0.20 rad/s`；速度、倾角、距离等仍由独立的
PX4 任务安全状态机监测并在越界时终止任务，不改写论文控制律。

静止目标的垂向几何也改为符合拦截运动逻辑的“从上向下接近”：目标中心保持
`3.0 m`，无人机拦截前先稳定在约 `4.0 m`；论文设计 LOS 从原先指向机体前上方的
`+20°` 改为指向前下方的 `-5°`。在 12 m 初始水平距离处，这对应约 `1.05 m`
的高度差。视觉获取阶段不再强制把目标放在光轴中心，而是对准该设计 LOS 在图像
中的位置；随着水平距离缩短，固定下视 LOS 会使无人机沿斜向下轨迹收敛到目标
高度并发生碰撞。该过程只使用预设试验高度、相机图像和 PX4 自身状态，不读取
目标 Gazebo 位姿或高度作为在线控制输入。

ROS 试验入口 `vision_direct_interception_trial.launch.py` 默认关闭飞行指令，只有
显式设置确认 token 后才向 PX4 发布 body-rate/thrust 指令。当前必须先重新完成
静止目标 SITL 碰撞确认，之后才能继续移动目标。

这里的无延迟闭环验收仅是确定性 plant/camera 回归基线。PX4/Gazebo 入口现在会
按 `capture_stamp` 引入 80 ms 延迟，并由 DKF 回溯校正后重放 IMU；不能把旧的
`D=0` 指标直接当成当前 `D=4` SITL 飞行结果。

---

## 7. 加入论文移动目标（代码保留，验收暂停）

静止目标完整闭环稳定后，再进入移动目标阶段。

优先按照论文 HITL 场景建立移动目标。

整个算法结构保持不变：

```text
Moving Target
      ↓
Camera
      ↓
Image Feature
      ↓
18维状态观测器
      ↓
p̂_r、p̄̂ᶦ、v̂_r + PX4 自身 Rᵉ_b
      ↓
论文 InterceptionController
      ↓
f_d、ω_d
      ↓
PX4
```

即使目标开始运动，也不能重新引入：

```text
目标 Gazebo 真实位置
目标 Gazebo 真实速度
Truth p_r
Truth v_r
```

### 实现结果

已修正此前在线链路中的关键偏差：`VisionInterceptionCoordinator` 不再在收到
PX4 odometry 后切换到另一套有界视觉速度控制。进入 `ACTIVE` 后，命令链唯一为：

```text
/interception/observer/state
        ↓
p̂_r、v̂_r、p̄̂ᶦ + PX4 自身 Rᵉ_b
        ↓
论文 Eq. (13)、(19)、(21)–(23)、(26)、(28)
        ↓
f_d、ω_d
        ↓
PX4 body-rate / thrust
```

PX4 自身 odometry 提供 Algorithm 1 的当前机体姿态，并用于速度/倾角安全门限、
碰撞后平滑退出和悬停恢复；它不提供任何目标状态。原始图像只用于起飞后的目标
搜索、对准和 observer 图像更新。

新增 `paper_moving_target.py` 和 `p7_moving_target.launch.py`：

| 项目 | 实现 |
| --- | --- |
| 论文 HITL 轨迹 | 等弧长速度的 8 字轨迹 |
| HITL 速度 | `5 / 7.5 / 10 m/s` 可配置，默认 `5 m/s` |
| 论文实飞轨迹 | 支持 `circle`，可配置 `6 m/s` |
| 启动时机 | 收到 observer reset 后启动，确保先完成起飞和视觉对准 |
| 碰撞后 | 收到绿色确认后停止轨迹驱动，让轻质目标自然脱离 |
| 仿真接口 | 只调用 Gazebo `/world/<world>/set_pose` |
| 算法接口 | 不发布目标 pose、velocity、odometry 或相对 Truth 状态 |

移动目标启动示例：

```bash
ros2 launch ibvs_sim p7_moving_target.launch.py \
  target_pattern:=figure8 \
  target_speed_m_s:=5.0
```

自动化验证包括：8 字/圆周轨迹起点与周期性、恒定弧长速度、无 Truth ROS 输出、
observer 对恒速移动目标的无延迟闭环，以及有 PX4 odometry 时仍由 observer 驱动
论文控制器。当前自动化测试和构建已通过；`5 / 7.5 / 10 m/s` 的 PX4/Gazebo
实际碰撞结果仍需按 `启动.md` 分档执行，不能在未跑飞行试验前宣称通过。
试验 launch 已将无人机自身的 `speed_limit_m_s` 和
`max_horizontal_distance_m` 暴露为显式参数；默认值仍保持静止目标的保守门限，
高速档必须人工逐档放宽，倾角、角速度、加速度和推力保护不变。

---

# 当前核心原则

```text
无人机自身 IMU / PX4 状态       → 可以使用
目标图像信息                    → 可以使用

目标 Gazebo odometry            → 删除
目标真实位置 / 速度             → 不作为算法输入
Truth p_r / v_r                 → 删除

p_r / v_r                       → 由 observer 作为状态估计

18维状态                        → 按论文建立
人工图像总延迟                  → 80 ms（可配置）
DKF 延迟步数 D                  → 4（按 50 Hz DKF 周期）
历史状态回溯 / 延迟校正          → 已加入；内部重放约 16 个 200 Hz IMU 子步

Section III Controller          → 使用估计的 p̂_r、p̄̂ᶦ、v̂_r + PX4 自身 Rᵉ_b
最终输出                        → f_d、ω_d

Contact + 目标变绿              → 唯一撞击确认；触发撞后退出流程
```

# 当前最优先任务

移动目标代码链路保留，但当前下一步是执行静止目标 SITL 稳定性复验。只有满足
“不翻转、Gazebo 碰撞确认、目标变绿、进入撞后稳定流程”后，才恢复移动目标分档验收。

下一步首先完成：

```text
① 清理 Truth（完成）
        ↓
② 建立论文 18 维状态的数据结构（完成）
        ↓
③ 按论文实现 D=4 历史 correction + IMU replay（完成）
        ↓
④ 验证 observer（完成）
        ↓
⑤ 将 observer 输出接入论文 Section III Controller（完成）
        ↓
⑥ 静止目标延迟闭环验收（D=4 待 SITL 复验）
        ↓
⑦ 加入论文移动目标（暂停，待静止目标通过后再做 5/7.5/10 m/s 验收）
```

当前下一步是使用 bag 核对 `capture_stamp → publish_stamp ≈ 80 ms`、
`reason=image_corrected_d4`，并完成静止目标 D=4 SITL 飞行复验。
