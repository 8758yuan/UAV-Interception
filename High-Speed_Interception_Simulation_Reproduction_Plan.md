# 《High-Speed Interception Multicopter Control by Image-Based Visual Servoing》仿真复现执行方案

## 1. 文档目的

本文档用于指导论文《High-Speed Interception Multicopter Control by Image-Based Visual Servoing》的仿真复现工作。复现工作基于现有的 Ubuntu 22.04、ROS 2 Humble、PX4 SITL 和 Gazebo Sim 环境完成。

复现目标分为三个层次：

1. **工程闭环复现**：实现基于 SO(3) 的图像视觉伺服（IBVS）拦截控制器、视觉延迟模拟和延迟卡尔曼滤波器（DKF），依次完成静止目标、运动目标和高速拦截。
2. **论文趋势复现**：通过控制变量实验验证“延迟增大会降低精度”、“DKF优于直接延迟视觉”、“50 Hz DKF优于低频DKF”及“目标速度升高后拦截误差增大”。
3. **论文数值逼近**：在结构和趋势复现后，再通过模型参数辨识、推力标定和控制器调参，逐步逼近论文的CEP数值。

由于论文使用 RflySim 硬件在环平台，而本项目使用 PX4 SITL 与 Gazebo，因此首先追求算法行为和统计趋势一致，再逐步逼近论文数值。论文的20 m/s是实飞终端速度结果，本项目将其视为核心统计实验完成后的扩展目标，不用它阻塞前期功能复现。

### 1.1 当前工程基线

当前工作区已形成可构建、可单元测试的 `interception_interfaces`、`ibvs_control` 和 `ibvs_sim` ROS 2功能包，具备P1飞控接口测试、P2真值状态链路、静止目标模型、SO(3)控制数学核心和只读ROS影子节点。SO(3)控制器尚未接入PX4输入，拦截闭环仍未启用。

项目已取得2次连续P1通过记录；严格阶段门仍为10次。根据2026-09-15的执行决定，工程提前开展不发送飞控指令的P2开发，但不将P1标记为正式通过，也不启用IBVS闭环或DKF。P2-A真值链路、P2-B控制纯函数和50/200 Hz离线闭环均已完成验证；工程初值在离线模型中32.07 s命中，峰值速度1.939 m/s、峰值倾角17.50°。ROS影子模式也已在线验证，`ControlDebug`约200 Hz且节点没有任何PX4输入发布者。P1悬停值0.727已形成首版线性推力映射，2 kg模型的归一化满推力暂估为26.9784 N；离线基线在该实际可用上限下仍安全命中。影子统计节点现可生成JSON报告，并通过“离地+12 s稳定等待”门禁排除地面和主要爬升数据。有效悬停基准 `p2_shadow_hover_01` 覆盖30 s和6001个样本：有效率100%、频率200 Hz、最小屏障裕量0.285、最大推力24.423 N/归一化0.905，物理推力与映射饱和率均为0。角速率限幅率为100%，符合影子模式不跟随期望姿态、姿态误差无法闭合的特性，只证明0.5 rad/s安全限幅持续有效，不能当作真闭环稳态饱和率。PX4指令适配纯函数、P2安全状态机和单一真值闭环协调节点均已实现：协调节点统一管理定点起飞、50/200 Hz控制、PX4坐标转换、切换预流、命中/超时终止、失联/超速/超倾角/屏障越界中止及自动降落。三重启用锁要求显式使能、确认令牌和P1通过数达到10；当前配置为禁用且P1为2/10，启动验证只进入调试模式，不创建PX4发布者。下一步是补齐剩余8次P1连续验收；阶段门通过后先执行协调节点的故障注入仿真，再允许首次低速真值闭环。

### 1.2 执行原则

- 每次只引入一个新变量：真值→相机→延迟→DKF→运动目标→高速。
- 控制器、坐标转换和DKF核心写成不依赖ROS消息的纯函数或类，先单元测试，再接ROS节点。
- DKF必须先用ROS bag离线回放验证，然后进入在线影子模式，最后才接入闭环。
- 每通过一个阶段门，就冻结代码版本、YAML参数和一份基准数据。
- 论文未公开的参数必须在项目中显式声明，不将自定义设置伪装成论文原参数。

---

## 2. 论文方法概述

论文的拦截系统不采用传统的“目标三维定位—轨迹规划—轨迹跟踪”结构，而是直接利用图像中的目标方向进行闭环控制。

系统由以下两部分构成：

1. **高速度IBVS拦截控制器**
   - 使目标视线向量与设计视线向量对齐；
   - 使相对速度方向与目标视线方向对齐；
   - 输出期望推力和机体系角速度；
   - 在大俯仰角和高速运动下保持目标处于相机视场内。

2. **延迟卡尔曼滤波器DKF**
   - 使用高频IMU数据预测状态；
   - 延迟图像到达后，在历史时刻修正状态；
   - 利用缓存IMU重新递推至当前时刻；
   - 补偿低帧率、视觉处理延迟和短时间目标丢失。

论文中的主要运行频率如下：

| 模块 | 频率或延迟 |
|---|---:|
| IMU与角速度指令 | 200 Hz |
| IBVS外环 | 50 Hz |
| 图像目标检测 | 30 Hz |
| DKF输出 | 10、30或50 Hz |
| 视觉延迟 | 约80 ms |

---

## 3. 仿真系统总体架构

### 3.1 ROS 2功能包

前期不拆成过多功能包，先用4个包形成可构建、可测试、可批处理的最小工程：

| 功能包 | 作用 |
|---|---|
| `interception_interfaces` | 定义图像特征、延迟测量、估计状态、控制调试和实验结果消息 |
| `ibvs_control` | Offboard状态机、PX4接口、坐标转换、SO(3)控制律和DKF核心 |
| `ibvs_perception` | 仿真相机接入、HSV目标检测、真值投影对照和视觉延迟模拟 |
| `ibvs_sim` | 目标模型与运动、Gazebo真值桥接、launch、批量实验、数据记录和绘图 |

当单个包内部已稳定且存在独立复用需求时，再将 `dkf_observer`、`experiment_manager` 等拆分为独立功能包。

### 3.2 数据流

```text
Gazebo相机 → 目标检测 → 延迟队列 → DKF → SO(3)外环(50 Hz)
PX4姿态、速度与IMU ───────────────────────────→ DKF/SO(3)控制器
SO(3)外环 → 角速度/推力发布(200 Hz) → PX4 SITL → Gazebo无人机
Gazebo目标真值 → 真值桥接 → 基线控制/评价/离线对照 → ROS bag/CSV
```

SO(3)外环计算 $\mathbf a_d$、$\mathbf R_d$ 和 $\boldsymbol\omega_1$；200 Hz内环根据当前姿态计算 $\boldsymbol\omega_2$，与最新的外环结果合成并发布 `VehicleRatesSetpoint`。PX4自带的机体角速度环仍然保留。

### 3.3 建议ROS 2话题

| 信息 | 建议话题或PX4话题 |
|---|---|
| 相机图像 | `/camera/image_raw` |
| 相机内参 | `/camera/camera_info` |
| 原始检测坐标 | `/interception/vision/raw_feature` |
| 延迟视觉坐标 | `/interception/vision/delayed_feature` |
| DKF估计状态 | `/interception/dkf/state` |
| DKF估计视线 | `/interception/dkf/los` |
| 控制调试量 | `/interception/control/debug` |
| PX4里程计 | `/fmu/out/vehicle_odometry` |
| PX4 IMU | `/fmu/out/vehicle_imu` 或 `/fmu/out/sensor_combined` |
| PX4控制模式 | `/fmu/in/offboard_control_mode` |
| PX4角速度与推力 | `/fmu/in/vehicle_rates_setpoint` |
| PX4飞行命令 | `/fmu/in/vehicle_command` |

实际PX4话题名称以当前 `px4_msgs` 版本为准。

### 3.4 核心接口约定

| 接口 | 最小字段 | 要求 |
|---|---|---|
| `VisionFeature` | `capture_stamp, publish_stamp, u, v, x_norm, y_norm, valid, seq` | DKF始终使用 `capture_stamp` 回溯历史状态 |
| `RelativeState` | `stamp, p_r, v_r, los, source` | `p_r=\mathbf p_I^e-\mathbf p_T^e`，`v_r=\mathbf v_I^e-\mathbf v_T^e`；`source` 标记 `truth/direct/dkf` |
| `DkfState` | `stamp, x, covariance_diag, los, valid, replay_ms` | 输出时刻表示已回放到的当前时刻 |
| `ControlDebug` | `z1, z2, ad, Rd, omega1, omega2, thrust, saturation_flags` | `z1`为标量、`z2/ad/omega1/omega2`为向量；支持调参、失败归因和饱和率统计 |
| `TrialResult` | `seed, success, reason, d_min, cep_components, intercept_time` | 每次仿真必须产生一条结果 |

---

## 4. 坐标系与接口规范

坐标系错误会直接导致无人机向相反方向倾斜，必须在控制器开发前完成验证。

建议统一规定：

- 控制算法内部世界坐标系：ENU；
- ROS机体系：FLU；
- 相机光学坐标系：右、下、前；
- PX4坐标系：NED与FRD；
- 所有ENU/NED和FLU/FRD转换集中放在 `px4_interface` 中；
- 相机与机体之间的固定旋转矩阵记为 $\mathbf R_c^b$，由Gazebo模型中的相机安装姿态确定。

程序中每个向量的变量名、消息字段或注释必须标明所在坐标系。坐标转换只能在统一的转换模块中实现，禁止在控制器内散落使用符号取反。

必须完成以下单元测试：

1. 目标处于图像中心时，归一化坐标为零；
2. 目标向图像右侧移动时，像素横坐标增大；
3. 目标向图像上方移动时，像素纵坐标减小；
4. 无人机前倾、侧倾和偏航时，目标像素变化方向正确；
5. PX4滚转、俯仰和偏航角速度指令方向正确；
6. 四元数转换后保持单位模且旋转方向一致。

### 4.1 时间基准

- 所有节点统一使用Gazebo/ROS仿真时间，不将系统墙钟与仿真时间混用；
- 视觉消息同时保留采集时间和发布时间；
- 延迟模拟以采集时间为基准，延后发布消息，不篡改原始采集时间；
- DKF通过采集时间定位历史状态，而不是默认“接收时间减去80 ms”；
- 批量仿真时记录仿真时间倍率和时钟跳变，场景重置后清空所有历史缓存。


### 4.2 论文符号与工程符号对齐原则

本方案以原论文公式和物理定义为最高依据。为提高代码可读性，工程实现允许使用更清晰的变量名，但不得改变论文变量的方向、坐标系、维度或控制含义。

- 论文中的拦截机位置/速度 \(^{e}\mathbf p,\,^{e}\mathbf v\)，工程文档可记为 \(\mathbf p_I^e,\mathbf v_I^e\)；
- 论文中的目标位置/速度 \(^{e}\mathbf p_t,\,^{e}\mathbf v_t\)，工程文档可记为 \(\mathbf p_T^e,\mathbf v_T^e\)；
- 论文严格定义 \(\mathbf p_r={}^{e}\mathbf p-{}^{e}\mathbf p_t\)、\(\mathbf v_r={}^{e}\mathbf v-{}^{e}\mathbf v_t\)，工程实现必须保持相同方向；
- 论文旋转矩阵 \(R_{F_1}^{F_2}\) 表示从坐标系 \(F_1\) 到 \(F_2\) 的旋转；本方案统一写作 \(\mathbf R_{F_1}^{F_2}\)，含义不变；
- 论文中的 \(^{i}\bar{\mathbf p}\) 是归一化图像特征，代码中对应 `p_bar` / `x_norm,y_norm`；
- 论文中的 \(D\) 是按IMU周期计的图像延迟步数，\(t_D\) 是延迟时间。工程实现中分别记作 \(d\) 和 \(\tau_v\)，并在注释中注明 `paper: D`、`paper: t_D`；
- 论文中的 \(^{b}\boldsymbol\omega_1\)、\(^{b}\boldsymbol\omega_2\)、\(^{b}\boldsymbol\omega_d\) 都在机体系表达，代码不得省略这一坐标系事实；
- 论文中的 \(f_d\) 是期望推力标量，\(\mathbf n_{fd}\) 是期望推力单位方向，两者不得混用。

### 4.3 统一符号约定

为避免“目标下标、时间变量、坐标系和向量方向”混淆，全文统一采用以下约定。论文已有核心符号尽量保留，但在工程实现中必须同时明确物理含义和坐标系。

| 符号 | 含义 | 统一约定 |
|---|---|---|
| \(e,b,c\) | 世界、机体、相机光学坐标系 | \(e\)：ENU；\(b\)：FLU；\(c\)：右-下-前 |
| \(\mathbf R_a^b\) | 旋转矩阵 | 将“在 \(a\) 系表达”的向量转换为“在 \(b\) 系表达” |
| \(\mathbf p_I^e,\mathbf p_T^e\) | 拦截机、目标位置 | 上标 \(e\) 表示均在世界系表达；\(I\) 为 interceptor，\(T\) 为 target |
| \(\mathbf v_I^e,\mathbf v_T^e\) | 拦截机、目标速度 | 与位置采用相同的下标规则 |
| \(\mathbf p_r\) | 相对位置 | 定义为 \(\mathbf p_I^e-\mathbf p_T^e\)，即“目标指向拦截机” |
| \(\mathbf v_r\) | 相对速度 | 定义为 \(\mathbf v_I^e-\mathbf v_T^e=\dot{\mathbf p}_r\) |
| \(\mathbf n_t\) | 目标视线单位向量 | 由拦截机指向目标，因此 \(\mathbf n_t=-\mathbf p_r/\|\mathbf p_r\|\) |
| \(\mathbf n_{td}\) | 设计/期望视线 | 与论文控制律保持一致 |
| \(\bar{\mathbf p}\) | 归一化图像坐标 | \(\bar{\mathbf p}=[\bar p_x,\bar p_y]^T\) |
| \((u,v)\) | 像素坐标 | \(u\) 向右增大，\(v\) 向下增大 |
| \(\mathbf a_d\) | 期望加速度 | 世界系表达 |
| \(f_d\) | 期望推力大小 | 标量；与“推力方向”分开表示 |
| \(\mathbf n_f,\mathbf n_{fd}\) | 当前/期望推力方向 | 单位向量 |
| \(\boldsymbol\omega_1,\boldsymbol\omega_2\) | 两部分角速度指令 | 分别对应视线对准项与姿态跟踪项 |
| \(\boldsymbol\omega_d\) | 最终期望机体系角速度 | 向量 |
| \(\omega_m\)（工程别名 \(\omega_{\max}\)） | 论文最大角速度 | 标量限幅值；论文式(28)(29)使用 \(\omega_m\) |
| \(z_1\) | 视线误差 | 标量 |
| \(\mathbf z_2\) | 相对速度误差 | 三维向量 |
| \(k_1,k_2\) | 控制增益 | 正标量 |
| \(k_b\) | 视场约束边界参数 | 正标量 |
| \(\tau_v\) | 视觉总延迟 | 单位为秒或毫秒，不再用 \(D\) 同时表示时间延迟和离散步数 |
| \(d\) | 离散延迟步数 | 仅在“历史索引 \(k-d\)”中使用 |
| \(\mathbf x,\hat{\mathbf x}\) | DKF真实/估计状态 | 18维状态向量 |
| \(\mathbf P,\mathbf Q,\mathbf R\) | 协方差、过程噪声、测量噪声矩阵 | 均使用大写粗体矩阵符号 |
| \(d_{\min}\) | 最小相对距离 | 一次试验中的最小 \(\|\mathbf p_r\|\) |
| \(r_{\mathrm{hit}}\) | 成功判定半径 | 第一版取0.5 m |
| \(t_{\mathrm{start}},t_{\mathrm{ca}}\) | 拦截开始、最近接时刻 | `ca` 表示 closest approach |

**书写规则：**

- 向量和矩阵在公式中使用粗体；标量使用普通斜体；
- 目标统一用大写下标 \(T\)，避免与时间变量 \(t\) 混淆；
- 坐标系信息优先写在上标中，例如 \(\mathbf p_I^e\)；
- ROS消息字段可继续使用 `p_r`、`v_r`、`los` 等简洁命名，但代码注释必须注明其数学定义和坐标系；
- 若论文原式使用不同符号，代码与报告中应建立一一映射，不在同一章节混用两套符号。


---

## 4.4 原论文核心公式到本方案的映射

| 论文公式 | 论文内容 | 本方案实现位置 | 复现要求 |
|---|---|---|---|
| (1)–(3) | 多旋翼刚体模型、推力模型、目标点质量模型 | P1/P2 | PX4/Gazebo承担飞行器动力学；相对状态定义必须与论文一致 |
| (4)–(6) | LOS、相机成像与IBVS图像雅可比 | 5.2、P3 | 相机坐标、归一化图像坐标和LOS方向必须逐项验证 |
| (7)–(11) | IMU模型、偏置随机游走、延迟图像测量 | P5/P6 | DKF前先验证时间戳、延迟和IMU输入 |
| (12)–(13) | LOS屏障Lyapunov函数与 \(\boldsymbol\omega_1\) | P2-B | 保留 \(k_b\) 约束，避免分母接近零 |
| (14)–(19) | 相对位置/速度Lyapunov设计与 \(\mathbf a_d\) | P2-B | 严格保持 \(\mathbf p_r,\mathbf v_r,\mathbf z_2\) 方向定义 |
| (20)–(23) | 期望推力方向、姿态构造和推力饱和 | P2-B | 区分 \(\mathbf n_{fd}\) 与 \(f_d\)，实现 \(0\le f_d\le f_m\) |
| (24)–(29) | SO(3)姿态误差、\(\boldsymbol\omega_2\)、总角速度饱和 | P2-B | 内环目标频率按论文200 Hz设计 |
| (30)–(31) | DKF状态和协方差预测 | P6 | 保存状态、协方差及IMU历史 |
| (32)–(36) | 延迟图像历史校正与重传播 | P6 | 在 \(k-d\) 校正，再利用缓存IMU传播至 \(k\) |

论文控制结构是“50 Hz共线外环 + 200 Hz姿态内环”；DKF用于以高于图像测量频率的速率提供当前图像状态估计。因此本方案的节点频率和数据流应围绕这一结构组织，而不是把IBVS简化成单一低频控制器。

## 5. 分阶段实施方案

> **编号约定**：`5.x` 是文档章节编号，`P0～P8` 是工程阶段编号。P2因内容较多拆为 `P2-A`（真值状态与闭环准备）和 `P2-B`（IBVS控制律与真值闭环），二者仍共同属于P2。5.2节的相机模型是IBVS公式所需的理论前置定义，但真正的相机接入仍在P3执行，因此工程顺序仍是 `P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7 → P8`。


|阶段|核心产出|阶段门|
|---|---|---|
| P0 基线冻结 | 可构建工程、版本清单、launch和YAML | 新环境可按文档复现启动 |
| P1 飞控接口 | Offboard状态机、角速度/推力接口和坐标测试 | 起飞悬停10次且控制方向全部正确 |
| P2 真值闭环 | P2-A真值状态定义 + P2-B SO(3)控制律与静止目标真值拦截 | 低速连续成功10次 |
| P3 相机几何 | 相机、红色目标检测、真值投影对照 | 像素方向、内外参和视场边界正确 |
| P4 无延迟视觉 | 图像视线闭环 | 固定工况成功率大于90% |
| P5 延迟基线 | 30 Hz采样和80 ms延迟队列 | 误差增大和相位滞后可重复 |
| P6 DKF | 离线回放、在线影子模式和DKF闭环 | 估计比延迟测量更接近当前真值 |
| P7 论文统计 | 有无DKF、DKF频率、目标速度实验 | 完成图8/图9对应结果并复现趋势 |
| P8 高速扩展 | 阻力、丢帧、抖动、风场和15～20 m/s测试 | 安全边界和失效模式明确 |

### 5.0 P0～P1：工程基线与飞控接口

#### P0 基线冻结

1. 创建4个ROS 2功能包的 `package.xml`、构建文件和基本目录；
2. 记录Ubuntu、ROS 2、PX4、`px4_msgs`、Gazebo和Micro XRCE-DDS版本；
3. 创建 `config/`、`launch/`、`test/`、`results/` 和实验元数据模板；
4. 核对当前PX4的输入输出话题名称和消息版本；
5. 提供一条命令启动PX4 SITL、Gazebo、通信桥和基本ROS节点。

#### P1 飞控接口

1. 将现有起飞脚本整理为 `INIT → PRESTREAM → OFFBOARD → ARM → TAKEOFF → HOLD → LAND/ABORT` 状态机；
2. 订阅 `VehicleStatus`和本地位置，以PX4回报判断模式切换、解锁和起飞成功；
3. 使用 `VehicleRatesSetpoint` 完成小幅滚转、俯仰、偏航角速度正负方向测试；
4. 标定悬停推力、最大可用推力和指令符号；
5. 实现通信超时、低高度、过大倾角和指令非有限值的保护。

P1未通过前，不向PX4发送由IBVS计算的闭环指令。

### 5.1 P2-A：真值状态与低速闭环准备

P2暂不使用相机、图像检测和DKF，直接读取Gazebo中的拦截机与目标真值：

$$
\begin{aligned}
\mathbf p_r &= \mathbf p_I^e-\mathbf p_T^e, \\
\mathbf v_r &= \mathbf v_I^e-\mathbf v_T^e=\dot{\mathbf p}_r, \\
\mathbf n_t &= -\frac{\mathbf p_r}{\|\mathbf p_r\|}.
\end{aligned}
$$

通俗解释：

- \(\mathbf p_I^e\) 和 \(\mathbf p_T^e\) 分别是拦截机和目标在世界系中的位置；
- \(\mathbf p_r\) 定义为“目标指向拦截机”的相对位置向量；
- \(\mathbf v_r\) 采用完全一致的方向定义，因此满足 \(\mathbf v_r=\dot{\mathbf p}_r\)；
- \(\mathbf n_t\) 必须由拦截机指向目标，所以它与 \(\mathbf p_r\) 方向相反，即 \(\mathbf n_t=-\mathbf p_r/\|\mathbf p_r\|\)。

这种定义虽然使 \(\mathbf p_r\) 与视线方向相反，但与后续 \(\mathbf v_{rd}=-k_1\mathbf p_r\) 和 \(\mathbf z_2=\mathbf v_r+k_1\mathbf p_r\) 保持同一套符号逻辑，避免在控制律中再次改变相对量方向。

#### 初始工况

- 拦截机高度：3 m；
- 目标位于相机光轴前方10～15 m；
- 横向偏移：1～2 m；
- 高度差：约1 m；
- 初始最大速度：2 m/s；
- 初始最大倾角：20°；
- 初始拦截成功阈值：0.5 m。

#### 阶段目标

- 验证PX4角速度和推力接口；
- 验证控制器符号、坐标系和饱和逻辑；
- 验证相对距离可以稳定收敛；
- 静止目标低速工况连续成功10次。

---

### 5.2 相机模型、图像坐标与目标观测
本节先定义IBVS控制律需要使用的相机几何、图像坐标和目标视线关系。这些定义属于控制器的理论与接口前置条件，并不表示P3已经开始接入真实图像闭环。

建议的第一版相机与目标参数如下：

| 参数 | 初始设置 |
|---|---:|
| 图像分辨率 | 640×480 |
| 水平视场角 | 120° |
| 相机帧率 | 30 Hz |
| 相机安装 | 前视、刚性固定 |
| 目标外观 | 红色球体 |

目标检测先采用OpenCV HSV颜色分割：

1. BGR图像转换到HSV空间；
2. 根据两段红色色相范围进行阈值分割；
3. 进行开运算和闭运算；
4. 提取最大有效轮廓；
5. 计算轮廓中心像素和面积；
6. 输出检测时间戳、像素坐标和有效标志。

归一化图像坐标为：

$$
\begin{aligned}
\bar p_x &= \frac{u-c_x}{f_x}, \\
\bar p_y &= \frac{v-c_y}{f_y}.
\end{aligned}
$$

通俗解释：$(u,v)$ 是检测到的目标像素位置，$(c_x,c_y)$ 是图像中心，$f_x,f_y$ 是相机在两个方向上的等效焦距。计算后得到的 $\bar p_x,\bar p_y$ 不再依赖图像分辨率，可以直接用于视觉伺服控制。

进入P3相机几何验证时，需要将检测坐标与Gazebo真值投影结果对比，确认相机内参和坐标方向正确。

上述相机模型在P2中只用于定义IBVS所需变量；真正的P3相机几何阶段仅被动记录图像和飞行状态，不让检测结果参与飞行控制。至少完成以下对照：

1. 随机选取不少于100个可见目标姿态，对比真值投影与HSV检测中心；
2. 记录像素误差和归一化坐标误差的均值、RMSE和最大值；
3. 验证图像四边、安全视场和目标失效标志；
4. 验证目标遮挡或离开视场时不输出伪坐标。

---



### 5.3 P2-B：IBVS控制律实现与真值闭环

#### 5.3.1 控制器输入

控制器需要以下状态：

- 归一化图像坐标 $\bar{\mathbf p}=[\bar p_x,\bar p_y]^T$；
- 相对位置向量 $\mathbf p_r=\mathbf p_I^e-\mathbf p_T^e$；
- 相对速度向量 $\mathbf v_r=\mathbf v_I^e-\mathbf v_T^e$；
- 无人机姿态矩阵 $\mathbf R_b^e$；
- 相机与机体固定外参 $\mathbf R_c^b$。

论文Algorithm 1虽然突出图像坐标、相对速度和姿态，但论文式（19）实际需要相对位置，因此程序接口必须保留 $\mathbf p_r$。

为避免将“图像闭环”与“完整状态估计闭环”混为一谈，各阶段的状态来源固定如下：

|运行模式|$\mathbf n_t$ 来源|$\mathbf p_r,\mathbf v_r$ 来源|用途|
|---|---|---|---|
| `truth` | Gazebo真值 | Gazebo真值 | 验证控制律和坐标系 |
| `vision_direct` | 无延迟或延迟图像 | 第一版使用Gazebo真值 | 隔离视觉/延迟对控制的影响 |
| `dkf_shadow` | DKF估计，不参与控制 | DKF估计 | 在线对照真值和延迟测量 |
| `dkf_closed_loop` | DKF估计 | DKF估计 | 最终论文方案闭环 |

最终结果中必须明确标记运行模式，真值只用于初始化和评价时不应被描述为“纯单目闭环”。

#### 5.3.2 目标视线

由归一化图像坐标计算世界坐标系中的目标视线（按4.3节旋转矩阵方向约定）：

$$
\mathbf n_t=\mathbf R_b^e\mathbf R_c^b
\frac{[\bar p_x,\bar p_y,1]^T}
{\left\|[\bar p_x,\bar p_y,1]^T\right\|}
$$

通俗解释：先用图像横、纵坐标和相机前向分量1组成相机坐标系中的视线，再依次通过相机外参 $\mathbf R_c^b$ 和无人机姿态 $\mathbf R_b^e$，把这条视线转换到世界坐标系。最后除以向量长度，将其变成单位方向向量。

第一版令设计视线 $\mathbf n_{td}$ 等于“当前机体姿态下的相机光轴在世界系中的方向”，而不是一个世界系常向量。控制稳定后，再实现论文提出的视场约束策略：在目标不离开安全视场的前提下，使设计视线尽量靠近期望推力方向。

#### 5.3.3 控制律

按照论文式（13）、（19）、（21）～（23）、（26）～（29）实现：

1. 计算视线误差

   $$
   z_1=1-\mathbf n_{td}^{T}\mathbf n_t
   $$

   其中，$n_{td}^Tn_t$ 是两个单位方向向量夹角的余弦。两条视线完全一致时，$z_1=0$；偏差越大，$z_1$ 越大。

2. 计算期望相对速度

   $$
   \mathbf v_{rd}=-k_1\mathbf p_r
   $$

   其中，$\mathbf v_{rd}$ 是期望相对速度，$k_1>0$ 是接近速度增益。由于 $\mathbf p_r$ 定义为“目标指向拦截机”，因此负号使期望相对速度指向目标，从而缩短相对距离。

3. 计算速度误差

   $$
   \mathbf z_2=\mathbf v_r+k_1\mathbf p_r
   $$

   $\mathbf z_2$ 表示实际相对速度 $\mathbf v_r$ 与期望相对速度 $-k_1\mathbf p_r$ 之间的三维向量误差。$\mathbf z_2$ 趋近于零时，无人机就在按照期望速度接近目标。

4. 由标量 $z_1$、向量 $\mathbf z_2$、$\mathbf p_r$ 和 $\mathbf v_r$ 计算期望加速度 $\mathbf a_d$；
5. 计算期望推力单位方向 $\mathbf n_{fd}$、期望姿态 $\mathbf R_d$ 和期望推力大小 $f_d$；
6. 计算视线对准角速度 $\boldsymbol\omega_1$ 和姿态跟踪角速度 $\boldsymbol\omega_2$；
7. 计算最终机体系角速度：

   $$
   \boldsymbol\omega_d=
   \operatorname{sat}\!\left(
   \boldsymbol\omega_1+\boldsymbol\omega_2,\,
   \omega_{\max}
   \right)
   $$

   通俗解释：把“让相机对准目标”的角速度 $\boldsymbol\omega_1$ 与“让无人机跟踪期望姿态”的角速度 $\boldsymbol\omega_2$ 相加，再用 $\omega_{\max}$ 限制最大角速度，避免指令超过飞行器能力。

#### 5.3.4 初始工程处理

论文没有公开全部控制参数，因此采用以下步骤：

- 第一版令目标加速度 $\mathbf a_T^e=\mathbf 0$，将目标机动视为外部扰动；
- 第一版忽略旋翼阻力 $\mathbf f_{\mathrm{drag}}$，控制稳定后再加入；
- $k_1,k_2$ 从较小值开始逐渐增加；
- 最大角速度按论文记号 $\omega_m$（代码可命名 `omega_max`）、最大推力 $f_m$ 和最大倾角必须限幅；
- 根据相机安全视场角初始设置 $k_b=1-\cos\theta_{safe}$，其中 $\theta_{\mathrm{safe}}$ 小于真实半视场角；
- 单独标定PX4归一化推力与悬停推力之间的关系；
- 式（22）中当前推力方向 $\mathbf n_f$ 与期望推力方向 $\mathbf n_{fd}$ 的旋转轴 $\mathbf n_f\times\mathbf n_{fd}$ 在代入Rodrigues公式前必须归一化，并单独处理夹角趋近0或 $\pi$ 的数值退化；
- 对 $\|\mathbf p_r\|$、$k_b^2-z_1^2$ 和期望推力模长设置数值下界，防止拦截末段出现除零。

#### 5.3.5 PX4输出

控制器通过以下消息向PX4发送指令：

- `OffboardControlMode`：启用机体系角速度控制；
- `VehicleRatesSetpoint`：发送滚转、俯仰和偏航角速度；
- `VehicleRatesSetpoint.thrust_body`：发送归一化机体系推力；
- `VehicleCommand`：完成解锁、进入Offboard以及必要的降落操作。

当前工程中的PX4适配器仍保持硬禁用。只有 `enable_control=true` 且确认令牌完全匹配时才会创建 `VehicleRatesSetpoint` 发布者；即使适配器启用，它本身也不负责进入Offboard、解锁或降落。在这些动作由同一个受测协调状态机统一管理前，不得单独启用适配器。

---

### 5.4 P3：仿真相机与目标检测验证

P2真值闭环通过后，进入P3。此阶段按照5.2节已经定义的相机模型、归一化图像坐标和目标视线关系，正式接入Gazebo相机与HSV目标检测，但检测结果仍不参与飞行控制。

P3的核心任务是验证：

1. 相机内参、外参以及相机光学坐标系方向正确；
2. HSV检测得到的像素坐标与Gazebo真值投影一致；
3. 归一化图像坐标 $\bar{\mathbf p}$ 的符号和变化方向正确；
4. 由图像坐标构造的目标视线 $\mathbf n_t$ 与真值视线方向一致；
5. 目标进入、接近和离开视场边界时，`valid` 标志正确。

P3通过后，才进入P4无延迟图像闭环，使图像观测真正参与IBVS控制。

---

### 5.5 P4：无延迟图像闭环

在不加人工延迟的情况下，使用检测图像坐标构造 $\mathbf n_t$，第一版仍由Gazebo真值提供 $\mathbf p_r,\mathbf v_r$。该模式定义为 `vision_direct`，用于隔离“相机/检测问题”与“状态观测问题”。

测试顺序：

1. 悬停时使用图像视线完成小角度对准；
2. 限速0.5 m/s完成首次接近；
3. 逐步将限速提高到1、2 m/s；
4. 在固定初始工况下完成50次基线运行。

P4验收要求为拦截成功率超过90%，且目标位于安全视场内的时间比例超过95%。

---

### 5.6 P5：视觉延迟基线

`delay_emulator` 使用带时间戳的消息队列模拟视觉处理过程。

初始设置：

- 相机与检测频率：30 Hz；
- 固定延迟：80 ms；
- 初期延迟抖动：0；
- 初期丢帧率：0%；
- 后续增加延迟抖动和丢帧。

依次完成以下测试：

| 工况 | 视觉延迟 | DKF |
|---|---:|---|
| D0 | 0 ms | 无 |
| D1 | 40 ms | 无 |
| D2 | 80 ms | 无 |
| D3 | 120 ms | 无 |

预期现象是：延迟越大、拦截速度越高，直接使用延迟图像坐标产生的拦截误差越大。

额外记录 `publish_stamp-capture_stamp` 的平均值、标准差和最大值。固定80 ms延迟的误差应在时钟分辨率允许范围内，不允许用ROS定时器误差代替延迟模型。

---

### 5.7 P6：延迟卡尔曼滤波器DKF

#### 5.7.1 状态定义

论文采用18维状态：

$$
\mathbf x=
\begin{bmatrix}
\mathbf q^T &
\mathbf p_r^T &
\mathbf v_r^T &
\bar{\mathbf p}^{\,T} &
\mathbf b_{\mathrm{gyr}}^T &
\mathbf b_{\mathrm{acc}}^T
\end{bmatrix}^T
$$

通俗解释：DKF把姿态、相对位置、相对速度、目标图像坐标以及两类IMU零偏合并成一个状态向量。各部分维数为 $4+3+3+2+3+3=18$。

其中包括：

- 姿态四元数4维；
- 相对位置3维；
- 相对速度3维；
- 归一化图像坐标2维；
- 陀螺仪零偏3维；
- 加速度计零偏3维。

#### 5.7.2 运行频率

- IMU状态预测：200 Hz；
- 图像修正：每次延迟图像到达时执行，约30 Hz；
- DKF估计输出：10、30或50 Hz可配置；
- IBVS外环：50 Hz；
- 角速度与推力指令发布：200 Hz。

论文所述的“DKF频率”在工程中统一定义为控制器可获得新DKF状态的频率。IMU预测仍保持200 Hz，图像修正仍在延迟图像到达时执行。实验中不通过同时改变IMU频率来制造10/30/50 Hz对照。

#### 5.7.3 延迟修正过程

保存最近至少0.3秒的以下数据：

- 状态估计 $\hat{\mathbf x}$；
- 协方差矩阵 $\mathbf P$；
- IMU角速度和加速度；
- 状态转移所需中间量；
- 所有数据对应的时间戳。

延迟图像到达时：

1. 由图像采集时间戳确定测量时刻 $t_m=t_k-\tau_v$；
2. 在离散缓存中找到与 $t_m$ 对应的历史索引 $k-d$，其中 $d$ 仅表示延迟步数；
3. 使用论文式（34）～（36）修正该历史状态和协方差；
4. 使用缓存IMU从 $k-d$ 重新递推至当前时刻；
5. 输出当前估计图像坐标、相对状态和视线方向；
6. 将当前估计提供给IBVS控制器。

历史修正后必须替换对应时刻之后的状态和协方差缓存，不能只生成一个当前输出而保留旧的历史分支。

#### 5.7.4 DKF接入顺序

1. **离线回放**：录制真值、IMU和带采集时间的图像特征，在不启动飞行控制的情况下调试状态传播、历史修正和重传播；
2. **在线影子模式**：DKF实时运行并输出估计，但控制器仍使用 `vision_direct`，对照DKF、延迟测量和当前真值；
3. **闭环切换**：只有当DKF无NaN/Inf、协方差正定、回放时间满足要求且估计误差优于延迟测量时，才切换到 `dkf_closed_loop`。

#### 5.7.5 工程检查

- 每次状态传播后归一化四元数；
- 四元数更新后选择与前一时刻连续的符号，避免 $\mathbf q$ 与 $-\mathbf q$ 跳变；
- 强制协方差矩阵保持对称；
- 检查协方差是否正定；
- 记录单次预测、修正和回放的计算耗时；
- DKF单周期计算时间应小于20 ms；
- 图像暂时丢失时仅执行预测；
- 超过设定丢失时间后退出拦截或进入安全模式。

由于单目相机不能直接确定绝对尺度，第一版使用Gazebo真值初始化 $\mathbf p_r$ 和 $\mathbf v_r$。后续再研究对初始深度误差的敏感性以及减少真值依赖的方法。

---

## 6. P7～P8：论文统计与高速扩展

正式批量实验前先用10个种子运行预实验，用于发现崩溃、超时和指标计算问题。预实验通过后冻结控制器、相机、延迟和DKF参数，再开始每组50次的正式实验。正式实验中不允许根据单次成败临时调参。

核心实验矩阵如下：

|编号|视觉延迟|DKF状态输出|目标工况|用途|
|---|---:|---:|---|---|
| E0 | 0 ms | 无 | 基准目标 | 理想视觉上限 |
| E1 | 80 ms | 无 | 基准目标 | 直接延迟视觉基线 |
| E2 | 80 ms | 50 Hz | 基准目标 | 有无DKF核心对照 |
| E3 | 80 ms | 10 Hz | 基准目标 | DKF频率对照 |
| E4 | 80 ms | 30 Hz | 基准目标 | DKF频率对照 |
| E5 | 80 ms | 50 Hz | 8字，5/7.5/10 m/s | 目标速度对照 |

E1与E2、E2与E3/E4以及E5的三种速度必须使用配对的随机种子和相同的初始条件。

### 6.1 实验一：有无DKF对比

#### 实验设置

- 使用0 ms无延迟视觉作为参考上限；
- 直接使用80 ms延迟视觉坐标；
- 使用50 Hz DKF估计坐标；
- 两组使用相同的50个随机种子；
- 每组运行50次；
- 其他控制器、目标轨迹和噪声设置完全相同。

#### 论文结果

| 方法 | 论文CEP |
|---|---:|
| 直接使用延迟视觉 | 0.408 m |
| 使用50 Hz DKF | 0.332 m |

最低复现要求：

$$
\mathrm{CEP}_{DKF}<\mathrm{CEP}_{direct}
$$

含义：使用DKF后的圆概率误差，应当小于直接使用延迟视觉坐标时的圆概率误差。

---

### 6.2 实验二：不同DKF频率

DKF分别以10、30和50 Hz输出，每种设置运行50次，并使用相同随机种子。

| DKF频率 | 论文CEP |
|---:|---:|
| 10 Hz | 0.425 m |
| 30 Hz | 0.423 m |
| 50 Hz | 0.332 m |

预期复现趋势：50 Hz DKF具有更小的CEP和更稳定的拦截结果。

10 Hz与30 Hz在论文中的CEP很接近，因此核心验收是50 Hz优于低频组，不强制要求30 Hz严格优于10 Hz。

---

### 6.3 实验三：不同目标速度

目标执行8字运动，DKF固定为50 Hz。目标速度分别为5、7.5和10 m/s，每组运行50次。

论文没有公开8字轨迹的准确尺寸，因此本项目需要固定并公开自己的轨迹参数。可采用：

$$
\begin{aligned}
p_{T,x}^e(t) &= p_{c,x}^e+A\sin(\omega_f t), \\
p_{T,y}^e(t) &= p_{c,y}^e+B\sin(2\omega_f t), \\
p_{T,z}^e(t) &= h_T.
\end{aligned}
$$

其中：

- \(\mathbf p_T^e(t)\) 是目标在世界系中的位置；
- \((p_{c,x}^e,p_{c,y}^e)\) 是8字轨迹中心；
- \(A\) 和 \(B\) 分别控制两个方向的轨迹尺寸；
- \(\omega_f\) 是8字轨迹参数频率，用下标 \(f\) 与机体角速度 \(\boldsymbol\omega\) 区分；
- \(h_T\) 是目标保持的飞行高度；
- \(t\) 仅表示仿真时间。

为了使目标沿轨迹保持设定速度，建议使用弧长参数化，而不是仅改变 $\omega$。

| 目标速度 | 论文CEP |
|---:|---:|
| 5 m/s | 0.330 m |
| 7.5 m/s | 0.371 m |
| 10 m/s | 0.390 m |

预期复现趋势：目标速度升高后，拦截误差略微增加，但DKF估计误差保持相对稳定。

---

### 6.4 实验四：单次拦截详细结果

选择一次具有代表性的成功拦截，绘制论文图9对应内容：

1. 拦截机三维轨迹；
2. 原始图像横、纵坐标与DKF估计坐标；
3. 拦截末段图像坐标局部放大；
4. 目标在图像平面中的运动轨迹；
5. 滚转、俯仰和偏航变化；
6. 拦截末段姿态局部放大；
7. 相对距离、速度、推力和角速度附加曲线。

应观察到DKF估计在目标图像坐标快速变化前提前响应，并减小80 ms视觉延迟造成的相位滞后。

---

### 6.5 实验五：旧IBVS方法对照

论文图10将新方法与作者早期IBVS方法进行对比。由于当前论文没有完整公开旧方法控制器，该实验放在主要结果复现完成之后。如需复现，应进一步阅读论文参考文献[11]并单独实现旧控制器。

这项实验不应阻塞图8和图9的复现。

### 6.6 P8：鲁棒性与高速扩展

P8不与论文核心结果复现混合。仅在E0～E5完成后，按下列顺序每次改变一个因素：

1. 延迟从80 ms扩展到40、120 ms；
2. 延迟抖动扩展到80±10 ms；
3. 丢帧率依次为5%、10%、20%；
4. 风扰动从0增加到2 m/s；
5. 加入旋翼阻力和完整机体限制；
6. 拦截速度以2 m/s为一级逐步提高，最终评估15～20 m/s。

每一级速度都必须检查成功率、最大倾角、角速度/推力饱和率、目标可见率和ABORT比例。如果饱和或ABORT持续上升，先确定失效边界，不通过取消安全限制来获得更高速度。

---

## 7. 随机实验规范

论文没有公开随机变量的具体分布，本项目采用可重复的统一设置，并将所有参数写入YAML配置文件。

建议初始设置如下：

| 随机量 | 建议设置 |
|---|---|
| 拦截机初始横向偏差 | 均匀分布±0.5 m |
| 初始高度偏差 | 均匀分布±0.3 m |
| 目标初始相位 | $[0,2\pi)$ 均匀分布 |
| 图像像素噪声 | 先关闭，后加入零均值高斯噪声 |
| 视觉延迟 | 先固定80 ms，后设置80±10 ms |
| 丢帧率 | 依次测试0%、5%、10%和20% |
| 风扰动 | 先关闭，后增加0～2 m/s风场 |

每次实验必须保存：

- 随机种子；
- Git提交号；
- PX4和Gazebo版本；
- 控制器与DKF参数文件；
- ROS bag；
- 结果CSV；
- 实验成功或失败原因。

进行控制变量对比时，除了被研究参数外，其他随机条件和参数必须保持一致。

---

## 8. 评价指标

### 8.0 事件定义

- `t_start` / $t_{\mathrm{start}}$：起飞稳定后，状态机进入 `INTERCEPT` 的时刻；
- `t_ca` / $t_{\mathrm{ca}}$：从 `t_start` / $t_{\mathrm{start}}$ 到任务结束之间相对距离最小的时刻；
- `SUCCESS`：在时间限制内 $d_{\min}\le r_{\mathrm{hit}}$，第一版 $r_{\mathrm{hit}}=0.5$ m；
- `MISS`：闭合速度已变为分离、超时或越界，且未达到成功阈值；
- `ABORT`：由安全状态机主动终止，必须保存唯一的失败原因码。

批量仿真中即使已达到 $r_{\mathrm{hit}}$，如果模型不发生实体碰撞，仍建议记录到距离达到最小值后再结束，以免 $d_{\min}$ 被成功阈值截断。

### 8.1 最小相对距离

$$
d_{\min}=\min_{t\in[t_{\mathrm{start}},\,t_{\mathrm{end}}]}
\left\|\mathbf p_r(t)\right\|
$$

通俗解释：在一次完整拦截过程中，逐时刻计算拦截机与目标之间的距离，其中最小的一个距离就是本次实验的脱靶距离 $d_{\min}$。

### 8.2 拦截成功率

建议第一版将 $d_{\min}\leq0.5\,\mathrm{m}$ 定义为成功。也就是说，两者最近距离不超过0.5 m，就记为一次成功拦截。论文没有给出统一碰撞半径，因此最终报告必须明确本项目使用的阈值。

### 8.3 圆概率误差CEP

以 $t_{\mathrm{ca}}$ 附近的相对闭合速度方向作为局部误差坐标系的纵轴，构造与之垂直的两个横向基向量，将 $\mathbf p_r(t_{\mathrm{ca}})$ 投影到该横向误差平面，得到 $(e_y,e_z)$。则：

$$
\mathrm{CEP}_{50}=\operatorname{median}
\left(\sqrt{e_{y,i}^2+e_{z,i}^2}\right)
$$

通俗解释：先计算每次实验在局部误差平面中的径向脱靶距离，再取所有脱靶距离的中位数。CEP越小，说明一半实验的拦截点越集中、总体精度越高。

论文对误差平面的工程构造交代不完整，因此报告中必须将上述定义标记为“本项目CEP口径”。同时报告 $\operatorname{median}(d_{\min})$，便于在不同误差平面定义下比较。CEP同时给出bootstrap 95%置信区间。

成功率必须基于全部样本计算；CEP默认也包含全部非ABORT样本。如果另外给出“仅成功样本CEP”，必须单独标注，避免通过删除MISS样本缩小误差。

### 8.4 其他指标

- 拦截时间；
- 终端速度；
- 最大俯仰角；
- 目标在相机视场内的时间比例；
- 目标丢失次数和最长连续丢失时间；
- DKF估计视线与真值视线的夹角；
- 原始视觉和DKF估计的均方根误差；
- 推力和角速度指令饱和比例；
- DKF平均和最大计算耗时。

---

## 9. 安全限制与异常处理

即使处于SITL阶段，也应保留与未来实飞兼容的安全状态机。

建议状态包括：

```text
INIT → TAKEOFF → SEARCH → INTERCEPT → SUCCESS
                            ↓
                         ABORT → HOLD/LAND
```

触发 `ABORT` 的条件包括：

- 目标连续丢失超过设定时间；
- 姿态超过安全倾角；
- 速度、角速度或推力持续饱和；
- DKF状态或协方差出现NaN/Inf；
- 无人机超出地理围栏；
- Offboard通信超时；
- 高度低于安全阈值。

---

## 10. 阶段验收标准

| 里程碑 | 验收条件 |
|---|---|
| P0 基线冻结 | ROS 2包可构建，一条命令可启动，版本和参数可追溯 |
| P1 飞控接口 | 起飞悬停连续成功10次，角速度方向正确，悬停推力已标定 |
| P2 真值闭环 | 静止目标在2 m/s限速下连续成功10次 |
| P3 相机几何 | 真值投影与检测坐标对照通过，视场边界和失效标志正确 |
| P4 无延迟视觉 | 图像闭环成功率超过90%，安全视场内时间超过95% |
| P5 延迟基线 | 80 ms延迟误差口径正确，且误差增大和相位滞后可重复 |
| P6 DKF | 离线、影子和闭环三步通过，估计比延迟测量更接近当前真值 |
| P7 论文统计 | 完成E0～E5，DKF组CEP低于直接延迟视觉组，50 Hz优于低频组 |
| P8 高速扩展 | 完成鲁棒性矩阵，给出15～20 m/s的稳定边界和失败原因 |

---

## 11. 论文未公开信息及处理原则

论文没有完整公开以下参数：

- 控制增益 $k_1,k_2,k_b$；
- 最大角速度 $\omega_{\max}$；
- DKF的 $\mathbf Q,\mathbf R,\mathbf P_0$；
- 完整无人机质量、惯量和旋翼阻力系数；
- 相机外参具体值；
- 8字轨迹尺寸；
- 随机扰动概率分布；
- 初始相对深度与速度获得方法；
- 设计视线 $\mathbf n_{td}$ 的完整工程构造算法；
- PX4推力指令映射关系；
- 统一的拦截成功判定半径。

因此，复现结果应分三级评价：

1. **结构一致**：控制器、DKF状态和延迟回放结构与论文一致；
2. **趋势一致**：DKF优于直接视觉，50 Hz优于低频，目标速度升高后误差增大；
3. **数值接近**：通过模型标定和参数调节，使CEP逐步接近论文结果。

不能在参数未公开的情况下，把CEP与论文存在少量差异直接判断为复现失败。

---

## 12. 推荐实施顺序

严格按照以下顺序开展：

1. **P0**：完成ROS 2包、版本冻结、launch、YAML和实验目录；
2. **P1**：稳定起飞/悬停/降落，完成坐标方向与角速度/推力标定；
3. **P2**：加入静止目标和真值桥，实现低速真值闭环；
4. **P3**：加入相机与红色目标检测，被动验证投影几何；
5. **P4**：在不加人工延迟时完成 `vision_direct` 闭环；
6. **P5**：加入30 Hz采样、40/80/120 ms延迟并冻结80 ms基线；
7. **P6**：按“离线回放→在线影子→DKF闭环”实现18维DKF；
8. **P7**：先建立批处理和指标管线，再完成E0～E5的正式实验和图表；
9. **P8**：最后测试延迟抖动、丢帧、风扰、阻力和15～20 m/s高速工况。

不应在第一阶段同时加入相机、DKF和高速控制。每完成一个里程碑后固定代码版本和参数，再进入下一阶段，以便快速定位故障来源。

---


## 12.1 全文符号一致性检查规则

后续代码、YAML、日志和论文复现报告均按照以下规则检查：

1. `p_r` 永远表示 \(\mathbf p_I^e-\mathbf p_T^e\)，禁止在某个节点中改成“目标减拦截机”；
2. `v_r` 永远满足 \(\mathbf v_r=\dot{\mathbf p}_r\)；
3. `los` 永远表示由拦截机指向目标的单位向量 \(\mathbf n_t\)；
4. `x_norm,y_norm` 对应 \(\bar p_x,\bar p_y\)，不与像素 \(u,v\) 混用；
5. \(\mathbf R_a^b\) 的含义始终是“\(a\) 系坐标 → \(b\) 系坐标”；
6. \(\boldsymbol\omega_d\) 是三维角速度向量，\(\omega_{\max}\) 是标量上限；
7. \(f_d\) 是推力大小，\(\mathbf n_{fd}\) 是推力方向，不使用同一符号同时表示方向和大小；
8. \(\tau_v\) 表示真实时间延迟，\(d\) 只表示缓存中的离散延迟步数；
9. 目标实体下标统一使用大写 \(T\)，时间只使用 \(t\)；
10. 图表、CSV列名和ROS消息字段必须能映射回本节符号表，避免后期统计时再次解释变量含义。


## 12.2 论文定义与工程自定义参数的边界

后续实现时必须给每个关键参数标记来源：

- `paper`：论文明确给出的定义、公式、频率或实验条件；
- `derived`：由论文公式直接推导得到；
- `engineering`：为PX4/Gazebo/ROS 2复现而新增的工程参数；
- `tuning`：需要通过仿真调节的控制或滤波参数。

特别注意：论文没有在正文中完整给出所有控制增益、噪声矩阵、PX4参数和Gazebo模型参数时，不得把本方案的建议值写成“论文参数”。这些值必须明确标为工程初值或待标定值。

## 13. 近期执行清单

当前第一个工作目标不是立即拦截，而是完成P0和P1，使飞控接口成为可信赖的控制底座。

### 13.1 P0任务包

- 将现有 `src/ibvs_control/offboard_takeoff.py` 纳入正式ROS 2功能包；
- 创建构建、安装、launch和YAML参数文件；
- 建立 `frames.py` 或等价的坐标转换模块及单元测试；
- 生成环境版本清单和当前PX4话题清单；
- 建立统一的日志、ROS bag和CSV输出目录。

### 13.2 P1任务包

- 将当前基于计数器的Offboard切换改为基于 `VehicleStatus` 回报的状态机；
- 保留位置模式起飞和悬停，增加安全降落与ABORT；
- 新增独立的角速度/推力测试模式，不在起飞逻辑中直接混入IBVS；
- 完成六个坐标与方向测试、悬停推力标定和10次重复起飞验证；
- 保存P1基准参数、ROS bag和测试记录。

### 13.3 P0～P1完成后的下一步

P0和P1通过后，再进入P2：在Gazebo中加入静止目标，读取无人机和目标真值，实现不依赖相机和DKF的SO(3)低速拦截闭环。

P2的第一个可交付结果是：在2 m/s限速下连续成功拦截静止目标10次，同时保存ROS bag、$d_{\min}$、饱和率和任务终止原因。只有P2稳定通过后，才开始接入仿真相机。
