# 算法公式与参数调节速查

> 以当前代码和 YAML 为准。主飞行链路：图像检测 → 18 状态延迟 EKF → 论文 SO(3) 拦截控制 → PX4 机体系角速度/推力。默认值来自 `vision_direct_interception.yaml` 与 `paper_state_observer.yaml`。

## 1. 符号与坐标系

- $e$：ENU 世界系；$b$：FLU 机体系；$c$：相机 optical（右、下、前）。
- $R_b^e$：机体系到世界系旋转矩阵；$q=[q_0,q_1,q_2,q_3]^T$：对应四元数。
- $p_r=p_{\rm interceptor}-p_{\rm target}$，$v_r=v_{\rm interceptor}-v_{\rm target}$。
- $n_t$：实际目标视线单位向量；$n_{td}$：设计视线单位向量。
- $[a]_{\times}$：向量 $a$ 的反对称矩阵；${\rm vex}(\cdot)$ 为其逆映射。
- $\operatorname{clip}_{[l,u]}(x)$：把数值 $x$ 限制在区间 $[l,u]$ 内：

$$
\operatorname{clip}_{[l,u]}(x)=
\begin{cases}
l, & x<l,\\
x, & l\le x\le u,\\
u, & x>u.
\end{cases}
$$

- $\operatorname{sat}_{\|\cdot\|\le a}(w)$：保持向量 $w$ 的方向，只把其长度限制为最大值 $a$：

$$
\operatorname{sat}_{\|\cdot\|\le a}(w)=
\begin{cases}
w, & \|w\|\le a,\\
\displaystyle a\frac{w}{\|w\|}, & \|w\|>a.
\end{cases}
$$

- $\operatorname{wrap}_{[-\pi,\pi)}(\psi)$：把角度 $\psi$ 换算为 $[-\pi,\pi)$ 范围内的等价角度。

## 2. 图像与视线

针孔模型与归一化像素：

$$
f_x=\frac{W}{2\tan(\mathrm{FOV}_h/2)},
\qquad
f_y=f_x,
\qquad
c_x=\frac{W-1}{2},
\qquad
c_y=\frac{H-1}{2}
$$

这里的 $f_y=f_x$ 是当前仿真相机的方形像素设定；使用真实相机时，$f_x$ 和 $f_y$ 应直接采用标定值。

$$x=\frac{u-c_x}{f_x},\qquad y=\frac{v-c_y}{f_y}$$

$$
n_c=\frac{\begin{bmatrix}x\\y\\1\end{bmatrix}}
{\left\|\begin{bmatrix}x\\y\\1\end{bmatrix}\right\|},
\qquad
n_t=R_b^eR_c^b n_c
$$

红色检测使用以下 HSV 条件：

$$
(h\le0.05\ \text{或}\ h\ge0.95)\quad\text{且}\quad s\ge0.45\quad\text{且}\quad v\ge0.25.
$$

目标中心是掩膜内所有像素坐标的平均值，目标面积是掩膜内像素的数量。

一阶低通（图像、角速度和推力均使用同型公式）：

$$
\alpha=1-e^{-\Delta t/\tau},\qquad
y_k=(1-\alpha)y_{k-1}+\alpha u_k
$$

## 3. 搜索与对准

图像误差：

$$e_I=\sqrt{x^2+y^2}$$

搜索时：

$$
\psi_{d,k+1}=\operatorname{wrap}_{[-\pi,\pi)}
\left(\psi_{d,k}+\omega_{search}\Delta t\right)
$$

发现目标后：

$$
\psi_{d,k+1}=\operatorname{wrap}_{[-\pi,\pi)}
\left(\psi_{d,k}+k_{\psi}x_k\Delta t\right)
$$

$$
z_{off,k+1}=\operatorname{clip}_{[-z_{max},z_{max}]}
\left(z_{off,k}+k_z y_k\Delta t\right)
$$

当 $e_I\le e_{center}$ 持续 $t_{settle}$ 后进入拦截；当 $e_I\ge e_{release}$ 时，稳定计时重新开始。

## 4. 18 状态延迟 EKF

状态：

$$
X=\begin{bmatrix}q\\p_r\\v_r\\\bar p_i\\b_g\\b_a\end{bmatrix}
\in\mathbb R^{18},
\qquad
\bar p_i=\begin{bmatrix}x\\y\end{bmatrix}
$$

初始化深度先验 $Z_0$：

$$
p_r(0)=-R_b^eR_c^b
\begin{bmatrix}xZ_0\\yZ_0\\Z_0\end{bmatrix}
$$

去偏 IMU：

$$\omega=\omega_m-b_g,\qquad f=f_m-b_a$$

四元数、速度和位置传播：

$$
q_k=\frac{M\!\left(\frac{1}{2}\omega\Delta t\right)q_{k-1}}
{\left\|M\!\left(\frac{1}{2}\omega\Delta t\right)q_{k-1}\right\|}
$$

其中，令

$$
\delta\boldsymbol{\theta}=\frac{1}{2}\omega\Delta t
=\begin{bmatrix}\delta\theta_1\\\delta\theta_2\\\delta\theta_3\end{bmatrix},
$$

则四元数增量矩阵为

$$
M(\delta\boldsymbol{\theta})=
\begin{bmatrix}
1&-\delta\theta_1&-\delta\theta_2&-\delta\theta_3\\
\delta\theta_1&1&\delta\theta_3&-\delta\theta_2\\
\delta\theta_2&-\delta\theta_3&1&\delta\theta_1\\
\delta\theta_3&\delta\theta_2&-\delta\theta_1&1
\end{bmatrix}.
$$

$$a_e=R_b^e f+g_e,\qquad v_{r,k}=v_{r,k-1}+a_e\Delta t$$

$$p_{r,k}=p_{r,k-1}+\tfrac12(v_{r,k-1}+v_{r,k})\Delta t$$

相机速度及 IBVS 图像传播：

$$v_c=(R_c^b)^T(R_b^e)^Tv_r,\qquad \omega_c=(R_c^b)^T\omega$$

$$
\bar p_{i,k}=\bar p_{i,k-1}
+\left(L_vv_c+L_\omega\omega_c\right)\Delta t
$$

$$L_v=\begin{bmatrix}-1/Z&0&x/Z\\0&-1/Z&y/Z\end{bmatrix},\quad
L_\omega=\begin{bmatrix}xy&-(1+x^2)&y\\1+y^2&-xy&-x\end{bmatrix}$$

协方差预测：

$$P_k^-=F_kP_{k-1}F_k^T+G_kQG_k^T$$

图像校正（观测矩阵 $H$ 只选取状态中的 $x,y$）：

$$
r_k=z_k-H\hat X_k^-,\qquad
S_k=HP_k^-H^T+R,\qquad
K_k=P_k^-H^TS_k^{-1}
$$

$$\hat X_k^+=\hat X_k^-+K_kr_k$$

$$
P_k^+=(I-K_kH)P_k^-(I-K_kH)^T+K_kRK_k^T
$$

异常帧门控：

$$\mathrm{NIS}_k=r_k^TS_k^{-1}r_k\le\gamma$$

图像延迟 $D$ 个滤波周期时，先在时刻 $k-D$ 进行校正，再按照保存的 IMU 子步把状态重放到时刻 $k$。

## 5. 论文 SO(3) 拦截控制（当前生效）

视线障碍误差和速度误差：

$$z_1=1-n_{td}^Tn_t$$

$$z_2=v_r+k_1p_r$$

障碍函数有效域：

$$
0\le z_1<k_b,\qquad
k_b=1-\cos(\theta_{safe})
$$

未限幅的期望加速度：

$$a_d^{\mathrm{raw}}=-k_1v_r-k_2z_2-p_r+
\frac{z_1}{k_b^2-z_1^2}\frac{m}{\|p_r\|}
(-I+n_tn_t^T)n_{td}+a_t$$

静止目标时 $a_t=0$，且期望相对速度为：

$$v_{r,d}=-k_1p_r=k_1\|p_r\|n_t$$

论文基线直接使用式 (19)，不对 $a_d$ 追加加速度限幅：

$$
a_d=a_d^{\mathrm{raw}}.
$$

期望合力、推力方向和期望姿态：

$$
F_d=m\left(a_d-g_e\right)-F_{drag},
\qquad
n_{fd}=\frac{F_d}{\|F_d\|}
$$

论文基线不改变 $n_{fd}$ 的方向，直接用最短的 SO(3) 旋转把当前推力轴

$$n_f=R_b^e e_3,\qquad e_3=\begin{bmatrix}0\\0\\1\end{bmatrix}$$

转到 $n_{fd}$，得到期望姿态 $R_d$。

视线项、姿态反馈项和总角速度：

$$\omega_1=\frac{z_1}{k_b^2-z_1^2}(R_b^e)^T(n_{td}\times n_t)$$

$$\omega_2=-\mathrm{vex}(R_d^TR-R^TR_d)$$

$$
\omega_d=\operatorname{sat}_{\|\cdot\|\le\omega_{max}}
\left(\omega_1+\omega_2\right)
$$

推力：

$$
T_{raw}=n_f^TF_d,
\qquad
T=\operatorname{clip}_{[0,T_{max}]}(T_{raw})
$$

角速度和推力命令不追加低通、最小推力或倾角回正，$n_{fd}$ 也不投影到额外的
倾角锥；这些旧的论文外分支已从实现与配置中删除。

## 6. PX4 推力映射与撞击后减速

由悬停标定估计满指令推力：

$$
T_{full}=\frac{mg}{u_{hover}},
\qquad
u_T=\operatorname{clip}_{[0,1]}
\left(\frac{T}{T_{full}}\right)
$$

PX4 FRD 推力命令为

$$
u_{FRD}=\begin{bmatrix}0\\0\\-u_T\end{bmatrix}
$$

当前 $m=2.0\,\mathrm{kg}$、$u_{hover}=0.727$，故 $T_{full}=26.9784\,\mathrm N$。

撞击后，先滑行 `post_hit_coast_duration_s`，再用三次 smoothstep 平滑停车：

$$
r(t)=\operatorname{clip}_{[0,1]}\left(\frac{t}{T_s}\right),
\qquad
h(r)=3r^2-2r^3
$$

$$
v_d(t)=\left[1-h(r(t))\right]v_0,
\qquad
a_d(t)=-\frac{6r(t)\left[1-r(t)\right]}{T_s}v_0
$$

## 7. 关键参数与调节方向

### 控制器

| 参数 | 当前值 | 增大后的主要效果 | 调节建议 |
|---|---:|---|---|
| `paper_k1` | 0.05 s⁻¹ | 期望接近速度增大；12 m 时约为 $0.6\,m/s$ | 速度太慢时小步增加；冲击过猛或制动反复时减小 |
| `paper_k2` | 3.0 s⁻¹ | 更快消除 $z_2$，但更易产生加速度反向和振荡 | 超调/急刹时减小；速度误差长期不收敛时增加 |
| `safe_los_angle_deg` | 60° | $k_b$ 增大，允许更大视线偏差 | 通常不动；减小更保守，但靠近边界时控制更激烈 |
| `designed_los_pitch_deg` | -5° | 更负时目标保持得更“下方”，飞行高度裕度增大 | 擦低目标时调得更负；飞得过高时向 0° 调 |
| `omega_limit_rad_s` | 0.20 | 姿态转得更快 | 跟踪慢时小幅增加；抖动、超调时减小 |

推荐顺序：先校准质量/悬停推力 → 固定安全终止门限 → 调 `paper_k1` → 调 `paper_k2` → 最后调 `omega_limit_rad_s`。

### EKF

| 参数 | 当前值 | 增大后的主要效果 | 调节建议 |
|---|---:|---|---|
| `initial_camera_depth_m` | 12.0 m | 初始估计距离更远 | 应按真实初始几何标定；它直接决定初始 $p_r$ |
| `dkf_delay_steps` / `dkf_update_rate_hz` | 4 / 50 Hz | 对应 80 ms 延迟 | 满足 $D\approx t_{delay}f_{DKF}$，须与相机延迟一致 |
| `gyro_noise_std_rad_s` | 0.015 | 更不信任陀螺传播，协方差增长更快 | 估计漂移且图像可靠时增加；估计过噪时减小 |
| `accel_noise_std_m_s2` | 0.15 | 更不信任加速度传播 | 速度漂移时增加；速度估计太抖时减小 |
| `image_noise_std` | 0.015 | 更不信任图像，校正更弱 | 像素跳动时增加；图像稳定但估计跟随慢时减小 |
| `maximum_image_innovation_nis` | 9.21 | 接受更多异常图像 | 误检多时减小；正常快速运动频繁被拒时增加 |
| `initial_position_std_m` | 0.5 m | 初期更允许图像修正位置 | 深度先验不准时增加；单目图像把距离拉飞时减小 |
| `minimum_depth_m` | 0.25 m | 增大后更早拒绝近距离传播 | 保持小正数；只在近距数值发散时适度增加 |
| `maximum_dt_s` | 0.05 s | 允许更长 IMU 间隔 | 这是异常保护，不用于改善控制；应大于正常周期且小于故障间隔 |

### 视觉获取与安全门限

| 参数 | 当前值 | 调节建议 |
|---|---:|---|
| `visual_search_yaw_rate_rad_s` | 0.20 | 搜索慢则增加；图像拖影或易越过目标则减小 |
| `visual_align_yaw_gain_rad_s` | 1.2 | 对准慢则增加；左右摆动则减小 |
| `visual_align_vertical_gain_m_s` | 0.4 | 垂直对准慢则增加；上下摆动则减小 |
| `visual_align_center_error` / `release_error` | 0.08 / 0.14 | 必须保持 $e_{release}>e_{center}$；误进 READY 则减小 center 或增加稳定时间 |
| `visual_align_settle_time_s` | 0.5 s | 增大更稳健但起飞攻击更慢 |
| `speed_limit_m_s` | 7.0 m/s | 属于仿真硬安全上限，不应拿来调控制品质 |
| `interception_tilt_limit_deg` | 55° | 覆盖论文报告约 50° 的峰值俯仰；超过即中止 |
| `minimum_barrier_margin` | 0.02 | 增大后更早中止视线越界 |

## 8. 备用/未接入主飞行节点的视觉 IBVS

`visual_ibvs.py` 当前只在测试中使用，不是 `vision_interception_coordinator` 的飞行控制律。其主要公式为：

$$
n_b=\frac{\begin{bmatrix}1\\-x\\-y\end{bmatrix}}
{\left\|\begin{bmatrix}1\\-x\\-y\end{bmatrix}\right\|},
\qquad
\omega_{LOS}=\left(k_{LOS}\frac{z_1}{k_b^2-z_1^2}+k_c\right)
R^T(n_{td}\times n_t)
$$

$$
c=\operatorname{clip}_{[0,1]}\left(\frac{A}{A_{term}}\right),
\qquad
v_{sched}=v_{cruise}+c\left(v_{term}-v_{cruise}\right)
$$

$$
\eta=\operatorname{clip}_{[0,1]}
\left(\frac{e_{stop}-e_I}{e_{stop}-e_{full}}\right),
\qquad
v_d=\eta v_{sched}
$$

$$
a_{LOS}=\operatorname{clip}_{[-a_{dec,max},a_{acc,max}]}
\left(k_v\left(v_d-v^Tn_t\right)\right)
$$

$$
v_\perp=v-(v^Tn_t)n_t,
\qquad
a_\perp=\operatorname{sat}_{\|\cdot\|\le a_{\perp,max}}
\left(-k_\perp v_\perp\right)
$$

$$
a_z=\operatorname{clip}_{[-a_{z,max},a_{z,max}]}(-k_zv_z),
\qquad
a_d=a_{LOS}n_t+a_\perp+
\begin{bmatrix}0\\0\\a_z\end{bmatrix}
$$

若以后切换到该控制律，再单独标定其 `los_rate_gain`、`speed_gain`、巡航/末端速度及横向/垂直阻尼；当前 YAML 没有这些生效参数。

## 9. 调参判断口诀

- **接近太慢**：先增 `paper_k1`，再看是否触及加速度上限。
- **中途急刹、来回反向**：减 `paper_k2` 或 `paper_k1`，不要先放宽上限。
- **姿态抖动**：减 `omega_limit_rad_s`，或增大两个滤波时间常数。
- **持续掉高**：核对悬停推力和质量，再增最小推力或垂直加速度上限。
- **目标在画面中摆动**：降低视觉对准增益；若是状态估计跳动，增大图像噪声或收紧 NIS 门限。
- **频繁触发保护**：先检查坐标系、相机标定、延迟和深度先验，不要直接放宽安全门限。
